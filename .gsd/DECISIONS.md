# Decisions Register

<!-- Append-only. Migrated from legacy project summaries. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | pattern | Legacy migration carry-over | Minimal landing page with deeper linked pages | Preserves clarity and low-friction feel while allowing broader work to be explored | Yes — if future milestones supersede it |
| D002 | M001 | pattern | Legacy migration carry-over | Organize deeper content by domains/themes | Better communicates capabilities across projects than a repo-by-repo gallery | Yes — if future milestones supersede it |
| D003 | M001 | pattern | Legacy migration carry-over | Use a casual lowercase voice | Matches the desired feel and makes the site read as more personal and direct | Yes — if future milestones supersede it |
| D004 | M001 | arch | Legacy migration carry-over | Build for GitHub Pages | Keeps hosting simple and aligned with deployment target | Yes — if future milestones supersede it |
| D005 | M001 | pattern | Legacy migration carry-over | Dark retro terminal aesthetic | Gives the site visual identity without heavy animation or gimmicks | Yes — if future milestones supersede it |
| D006 | M001 | pattern | Legacy migration carry-over | Dist-first validation (6 phase validators) | Gates catch regressions in built output before deploy | Yes — if future milestones supersede it |
| D007 | M001 | pattern | Legacy migration carry-over | Domain-first homepage (not personal-first) | Homepage acts as orientation layer into domains rather than a personal statement | Yes — if future milestones supersede it |
| D008 | M001 | pattern | Legacy migration carry-over | Inline flagships (not standalone case studies) | Deepens domain hubs without undoing the information architecture | Yes — if future milestones supersede it |
| D009 | M002 | arch | Portfolio protection model | Simple static passcode gate on protected routes | GitHub Pages hosting cannot support real server-side auth; a lightweight deterrent matches the requested scope | Yes — if hosting or security needs change |
| D010 | M002 | scope | Protected route scope | Protect `/domains/*` only | Keeps the deep proof layer gated while preserving the public discovery surfaces | Yes — if notes or other surfaces should later be included |
| D011 | M002 | pattern | Unlock persistence | Session-scoped unlock across protected routes | Avoids repeated prompts while keeping access temporary to the current browser session | Yes — if a different persistence model becomes necessary |
| D012 | M002 | scope | Public route allowlist | Keep `/`, `/about/`, and `/resume/` public | The user explicitly wants public positioning and resume access to remain open | No |
| D013 | M002/S01 | pattern | Boundary verification markers | Expose stable `data-route-visibility`, `data-gate-state`, and locked-shell markers on public/protected route renders | The route split needs deterministic DOM hooks for dist validation, browser smoke tests, and later unlock-state work | Yes — if a stronger verification surface replaces DOM markers later |
| D014 | M002/S01 | verification | Boundary regression gate | Prove S01 with both built-artifact assertions and a real browser cold-load test, then wire both into `validate:site` before deploy | This slice changes static HTML output and runtime entry behavior, so artifact-only checks are not enough | Yes — if the release gate architecture changes later |
| D015 | M002/S01/T01 | observability | Locked-boundary marker contract | Treat `data-route-visibility="protected"`, `data-gate-state="locked"`, `data-protected-gate`, and `data-protected-proof-state="withheld"` as the required cold-load markers for protected domain routes | The new Node and browser regression tests need one explicit locked-state contract so failures point to missing markers instead of vague page-shape drift | Yes — if a later slice intentionally revises the marker vocabulary |
| D016 | M002/S01/T02 | pattern | Shared domain boundary seam | Keep `/domains/[slug]` routed through `DomainPage` with an explicit `gateState` switch so the cold-load locked shell and later unlocked proof view share one rendering seam | This preserves a single domain-route entry path, prevents protected proof from leaking into initial HTML now, and gives later slices a clean place to wire session unlock behavior without duplicating route markup | Yes — if future slices move the gate boundary to a different abstraction |
| D017 | M002/S02 | verification | Unlock-flow release gate | Extend `validate:site` with both a fast dist validator for locked-shell messaging and a real browser warm-session unlock test before deploy | S02 changes both shipped protected HTML and stateful browser behavior, so release proof must cover cold-load contract plus same-session continuity | Yes — if the release gate architecture changes later |
| D018 | M002/S02 | pattern | Client unlock seam | Validate passcodes against a build-time public hash, persist a versioned `sessionStorage` unlock marker, and mount unlocked proof through a shared client-renderable domain view-model behind `DomainPage` | This keeps S01 cold-load HTML clean, avoids shipping the literal passcode directly, and reuses one protected-route seam for current-page unlock plus cross-route continuity | Yes — if hosting or protection requirements change later |
| D019 | M002/S02/T02 | pattern | localStorage cross-tab bridge for session unlock | Write unlock marker to both `sessionStorage` and `localStorage` on unlock; on page load, check sessionStorage first then promote from localStorage if present. Fresh incognito contexts get empty localStorage so relock works. | `sessionStorage` is per top-level browsing context (per-tab), so a new tab in the same browser session starts with empty sessionStorage. Cross-tab carryover requires a storage mechanism shared across tabs for the same origin. | Yes — if the unlock persistence model changes |
| D020 | M002/S03 | pattern | CSS-driven blur-to-clear reveal with `data-visual-state` markers | Mount proof DOM with `data-visual-state="revealing"` (blur + opacity 0), flip to `"revealed"` after a frame separation so the browser paints the blurred state before transitioning to clear over ~400ms | Keeps reveal lightweight (pure CSS transitions, no runtime deps), produces verifiable DOM hooks for S04 browser tests, and layers on top of existing gate markers without changing S02 timing | Yes — if a different reveal treatment is desired |
| D021 | M002/S03 | pattern | Gallery JS re-initialization via `astro:page-load` dispatch after dynamic proof mount | After `mountProof()` renders screenshot gallery DOM, dispatch `astro:page-load` event so `ScreenshotGallery.astro`'s inline script re-initializes carousel/lightbox for dynamically added galleries | The gallery init script already listens for `astro:page-load`; dispatching it avoids duplicating initialization logic or extracting `initGalleries()` into a shared module | Yes — if gallery initialization is refactored into a shared import |
| D022 | M002/S04 | verification | S04 verification strategy: assembled flow test + notes isolation guard | Add one end-to-end browser test exercising the full lock→error→unlock→reveal→cross-route journey and one notes isolation guard, then wire both into `validate:site` | The existing 17 browser tests cover individual behaviors but no single test proves the assembled visitor journey; notes routes need a regression guard against accidental gating (R303) | Yes — if the verification strategy evolves |
| D023 | M003 | arch | Shader implementation approach | Custom WebGPU (WGSL) + WebGL2 (GLSL) from scratch — no third-party shader library | User explicitly wants handmade GPU code, not a library dependency; reinforces technical craft positioning | No |
| D024 | M003 | pattern | Shader visual style | Faded dither — animated gradient blobs through Bayer ordered dithering | Matches retro terminal aesthetic (D005); inspired by shaders.com/collection/faded-dither but built from scratch | Yes — if visual direction changes |
| D025 | M003 | arch | Shader color palette source | Derive from CSS custom properties (--bg, --accent, --accent-strong) | Keeps shader cohesive with site identity without hardcoding colors | Yes — if palette management changes |
| D026 | M003 | pattern | Shader scope | All pages via BaseLayout.astro with per-page opt-out prop | User wants site-wide presence but easy selective disabling | Yes — if pages should have different shader configs |
