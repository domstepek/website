---
phase: 03-homepage-positioning
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/home.ts
  - src/components/home/HomePage.astro
  - src/pages/index.astro
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
user_setup: []
must_haves:
  truths:
    - The homepage replaces the Phase 1 placeholder with a real first-screen introduction that explains the kinds of systems Dom builds and routes into the five v1 domain pages.
    - The homepage renders all five domain pages as the primary internal next step using the shared Phase 2 domain registry instead of a separate project gallery or hand-copied link list.
    - GitHub, LinkedIn, and email links are visible on the homepage, and a freshness signal such as `currently` or `last updated` is rendered in homepage content.
  artifacts:
    - path: src/data/home.ts
      provides: Homepage-specific copy, contact links, and freshness content
      exports:
        - homePage
    - path: src/components/home/HomePage.astro
      provides: Shared homepage markup for hero, domain navigation, contact links, and freshness signal
    - path: src/components/home/HomePage.astro
      provides: Stable homepage root marker
      contains: "data-home-page"
    - path: src/components/home/HomePage.astro
      provides: Stable homepage hero marker
      contains: "data-home-hero"
    - path: src/components/home/HomePage.astro
      provides: Stable domain navigation marker
      contains: "data-home-domain-nav"
    - path: src/components/home/HomePage.astro
      provides: Stable contact-link marker
      contains: "data-home-contact-link"
    - path: src/components/home/HomePage.astro
      provides: Stable freshness marker
      contains: "data-home-freshness"
    - path: src/pages/index.astro
      provides: Homepage route wired through BaseLayout and the shared homepage component
      contains: "HomePage"
    - path: src/pages/index.astro
      provides: Homepage metadata handoff through the shared layout
      contains: "canonicalPath"
  key_links:
    - from: src/components/home/HomePage.astro
      to: src/data/home.ts
      via: Renders homepage-specific intro, contact, and freshness content from one data module
      pattern: "homePage"
    - from: src/components/home/HomePage.astro
      to: src/data/domains/index.ts
      via: Renders primary domain navigation from the shared Phase 2 registry
      pattern: "domains"
    - from: src/components/home/HomePage.astro
      to: src/lib/paths.ts
      via: Uses base-aware internal paths for the homepage-to-domain links
      pattern: "domainPath"
    - from: src/pages/index.astro
      to: src/components/home/HomePage.astro
      via: Replaces the placeholder homepage copy with the shared homepage component
      pattern: "HomePage"
---

<objective>
Replace the placeholder homepage with a reusable text-first homepage component and data source.

Purpose: Lock the visitor-facing landing experience now that the domain architecture exists, without widening into flagship storytelling or later personal-context work.
Output: A shared homepage data module, a `HomePage.astro` component, and a real `src/pages/index.astro` route wired through `BaseLayout`.
</objective>

<execution_context>
@/Users/jstepek/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jstepek/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-homepage-positioning/03-RESEARCH.md
@.planning/phases/03-homepage-positioning/03-VALIDATION.md
@src/pages/index.astro
@src/components/layout/BaseLayout.astro
@src/data/site.ts
@src/data/domains/index.ts
@src/lib/paths.ts
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define homepage-specific content and outbound-link data</name>
  <files>src/data/home.ts</files>
  <action>Create a dedicated `src/data/home.ts` module that holds the homepage eyebrow or intro copy, lead paragraph, contact links, and freshness content in the site's direct lowercase voice. Keep the contact shape explicit enough to distinguish GitHub, LinkedIn, and email, and keep the freshness value intentional and manually meaningful. Do not duplicate domain summaries or create a second homepage-only domain taxonomy in this file.</action>
  <verify>`pnpm astro check` passes after the new data module compiles cleanly.</verify>
  <done>The homepage has one source of truth for intro copy, contact links, and freshness state.</done>
</task>

<task type="auto">
  <name>Task 2: Build a shared homepage component around the existing domain registry</name>
  <files>src/components/home/HomePage.astro</files>
  <action>Add `src/components/home/HomePage.astro` and have it import `homePage`, the ordered `domains` registry, and `domainPath(slug)`. Render a text-first hero, a primary domain-navigation block using the five existing domain entries, a visible contact-link cluster, and a freshness note. Include stable `data-*` markers for the homepage root, hero, domain navigation, contact links, and freshness so Phase 3 can validate built output later. Avoid cards, client-side interaction, or any project-gallery section.</action>
  <verify>`pnpm astro check && pnpm astro build` succeed and the built homepage renders the new hero and domain links without route or component errors.</verify>
  <done>The repo has one reusable homepage component that turns the domain registry into real homepage navigation.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire the homepage route through BaseLayout and the shared component</name>
  <files>src/pages/index.astro</files>
  <action>Replace the Phase 1 placeholder content in `src/pages/index.astro` with a thin route that passes homepage-specific title, description, and `canonicalPath` into `BaseLayout`, then renders `HomePage`. Remove the placeholder copy entirely. Keep metadata and internal links base-path-aware and do not pull in flagship sections, screenshots, or resume-like content.</action>
  <verify>`pnpm astro check && pnpm astro build` succeed and `dist/index.html` is generated from the new shared homepage component.</verify>
  <done>The landing route is no longer a temporary placeholder and now serves as the real Phase 3 homepage entry point.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm astro check && pnpm astro build` succeeds
- [ ] The placeholder Phase 1 homepage copy is removed
- [ ] The homepage includes a real hero, five domain links, visible contact links, and a freshness marker
- [ ] Internal domain links use `domainPath(slug)` rather than raw root-relative paths
- [ ] No project-gallery, flagship, or Phase 5 personal-context sections were pulled into the homepage
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The site now has a real homepage structure built from shared data and the Phase 2 domain registry rather than a temporary placeholder
</success_criteria>

<output>
After completion, create `.planning/phases/03-homepage-positioning/03-01-SUMMARY.md`
</output>
