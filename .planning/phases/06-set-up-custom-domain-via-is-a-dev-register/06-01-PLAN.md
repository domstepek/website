---
phase: 06-set-up-custom-domain-via-is-a-dev-register
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - astro.config.mjs
  - src/data/site.ts
  - .github/workflows/deploy.yml
  - public/CNAME
autonomous: true
requirements: []

must_haves:
  truths:
    - "Site builds under root base path `/` with the new domain as canonical origin"
    - "CNAME file exists in public/ with exactly `jean-dominique-stepek.is-a.dev`"
    - "CI workflow defaults reference the new domain and root base path"
    - "Site description is visitor-facing in casual lowercase voice, not repo-style"
  artifacts:
    - path: "public/CNAME"
      provides: "GitHub Pages custom domain declaration"
      contains: "jean-dominique-stepek.is-a.dev"
    - path: "astro.config.mjs"
      provides: "Updated DEFAULT_SITE_URL and DEFAULT_BASE_PATH"
      contains: "jean-dominique-stepek.is-a.dev"
    - path: "src/data/site.ts"
      provides: "Updated defaults and visitor-facing description"
      contains: "jean-dominique-stepek.is-a.dev"
    - path: ".github/workflows/deploy.yml"
      provides: "Updated CI env var fallback defaults"
      contains: "jean-dominique-stepek.is-a.dev"
  key_links:
    - from: "astro.config.mjs"
      to: "src/data/site.ts"
      via: "Both files must declare identical DEFAULT_SITE_URL and DEFAULT_BASE_PATH values"
      pattern: "jean-dominique-stepek\\.is-a\\.dev"
    - from: "public/CNAME"
      to: "dist/CNAME"
      via: "Astro copies public/ to dist/ at build time"
      pattern: "jean-dominique-stepek\\.is-a\\.dev"
---

<objective>
Update site config defaults from `jstepek.github.io/website` to `jean-dominique-stepek.is-a.dev` with root base path, add the CNAME file for GitHub Pages, refresh the site description, and update CI workflow defaults.

Purpose: This is the core config transition that makes the entire site aware of the new custom domain. All canonical URLs, OG tags, internal routes, and CI builds derive from the two default constants being changed here.
Output: Updated config files, CNAME file, CI workflow with new defaults.
</objective>

<execution_context>
@/Users/jstepek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jstepek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-set-up-custom-domain-via-is-a-dev-register/06-CONTEXT.md
@.planning/phases/06-set-up-custom-domain-via-is-a-dev-register/06-RESEARCH.md

<interfaces>
<!-- Key constants and config surfaces being modified -->

From astro.config.mjs:
```javascript
const DEFAULT_SITE_URL = "https://jstepek.github.io";
const DEFAULT_BASE_PATH = "/website";
```

From src/data/site.ts:
```typescript
const DEFAULT_SITE_URL = "https://jstepek.github.io";
const DEFAULT_BASE_PATH = "/website";

export const siteConfig = {
  name: "dom",
  defaultTitle: "dom | systems, products, and tooling",
  defaultDescription:
    "a static astro foundation for a domain-first personal site about systems, products, and developer tooling.",
  siteUrl: resolveSiteUrl(import.meta.env.PUBLIC_SITE_URL),
  basePath: normalizeBasePath(import.meta.env.PUBLIC_BASE_PATH ?? DEFAULT_BASE_PATH),
  defaultSocialImage: "og-default.png",
  defaultFavicon: "favicon.svg",
} as const;
```

From .github/workflows/deploy.yml:
```yaml
env:
  PUBLIC_SITE_URL: ${{ vars.PUBLIC_SITE_URL || format('https://{0}.github.io', github.repository_owner) }}
  PUBLIC_BASE_PATH: ${{ vars.PUBLIC_BASE_PATH || format('/{0}', github.event.repository.name) }}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update site config defaults and add CNAME file</name>
  <files>astro.config.mjs, src/data/site.ts, public/CNAME</files>
  <action>
    1. In `astro.config.mjs`, change `DEFAULT_SITE_URL` from `"https://jstepek.github.io"` to `"https://jean-dominique-stepek.is-a.dev"` and `DEFAULT_BASE_PATH` from `"/website"` to `"/"`. Leave everything else unchanged — the `normalizeBasePath` helper already handles `"/"` correctly.

    2. In `src/data/site.ts`, make the same two constant changes: `DEFAULT_SITE_URL` to `"https://jean-dominique-stepek.is-a.dev"` and `DEFAULT_BASE_PATH` to `"/"`. Also update `defaultDescription` from the current repo-style description to a visitor-facing description in the site's casual lowercase voice. Something like: `"dom builds analytics platforms, infrastructure, ai/ml tooling, product systems, and developer experience tooling."` — this matches the site's existing tone. Keep `name`, `defaultTitle`, and all other fields unchanged per the locked decision that the site identity stays "dom" everywhere.

    3. Create `public/CNAME` containing exactly one line: `jean-dominique-stepek.is-a.dev` with no trailing newline. This file gets copied to `dist/CNAME` by Astro's static build, which is what GitHub Pages reads for custom domain configuration.

    IMPORTANT: Both files must have identical `DEFAULT_SITE_URL` and `DEFAULT_BASE_PATH` values. A mismatch between astro.config.mjs and src/data/site.ts will cause canonical URLs to disagree with the Astro site/base config.
  </action>
  <verify>
    <automated>cd /Users/jstepek/Personal\ Repos/website && grep -q 'jean-dominique-stepek.is-a.dev' astro.config.mjs && grep -q 'jean-dominique-stepek.is-a.dev' src/data/site.ts && grep -q '"/"' astro.config.mjs && grep -q '"/"' src/data/site.ts && test "$(cat public/CNAME)" = "jean-dominique-stepek.is-a.dev" && echo "Config defaults and CNAME verified"</automated>
  </verify>
  <done>Both config files declare `https://jean-dominique-stepek.is-a.dev` as DEFAULT_SITE_URL and `"/"` as DEFAULT_BASE_PATH. Site description is visitor-facing. public/CNAME contains the exact custom domain.</done>
</task>

<task type="auto">
  <name>Task 2: Update CI workflow defaults and verify full site build</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
    1. In `.github/workflows/deploy.yml`, replace the `env` block fallback expressions in the `build` job:
       - Change `PUBLIC_SITE_URL` fallback from `format('https://{0}.github.io', github.repository_owner)` to the static string `'https://jean-dominique-stepek.is-a.dev'`
       - Change `PUBLIC_BASE_PATH` fallback from `format('/{0}', github.event.repository.name)` to the static string `'/'`
       - Keep the `vars.*` override mechanism intact: `${{ vars.PUBLIC_SITE_URL || 'https://jean-dominique-stepek.is-a.dev' }}`

    The updated env block should be:
    ```yaml
    env:
      PUBLIC_SITE_URL: ${{ vars.PUBLIC_SITE_URL || 'https://jean-dominique-stepek.is-a.dev' }}
      PUBLIC_BASE_PATH: ${{ vars.PUBLIC_BASE_PATH || '/' }}
    ```

    2. After updating the workflow, run `pnpm build` locally to verify the site builds successfully under the new root base path. Then run the existing `pnpm validate:site` (phases 1-5) to confirm no regressions — the validators should pass because they derive expected paths from the emitted canonical URL, which now uses the new domain and root base path.
  </action>
  <verify>
    <automated>cd /Users/jstepek/Personal\ Repos/website && pnpm build && pnpm validate:site</automated>
  </verify>
  <done>CI workflow defaults reference the new domain and root base path with vars override intact. Site builds cleanly under root base path. All Phase 1-5 validators pass with no regressions.</done>
</task>

</tasks>

<verification>
- `grep -c 'jean-dominique-stepek.is-a.dev' astro.config.mjs src/data/site.ts .github/workflows/deploy.yml public/CNAME` shows matches in all four files
- `pnpm build` succeeds with no errors
- `pnpm validate:site` passes all Phase 1-5 checks under the new base path
- `cat public/CNAME` outputs exactly `jean-dominique-stepek.is-a.dev`
- Built `dist/CNAME` contains the domain (Astro copies public/ to dist/)
</verification>

<success_criteria>
Site config fully transitioned to `jean-dominique-stepek.is-a.dev` with root base path `/`. All existing validators pass. CNAME file present in both source and build output. CI workflow defaults updated.
</success_criteria>

<output>
After completion, create `.planning/phases/06-set-up-custom-domain-via-is-a-dev-register/06-01-SUMMARY.md`
</output>
