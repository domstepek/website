---
phase: 01-publishing-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/components/layout/BaseLayout.astro
  - src/styles/global.css
  - src/pages/index.astro
  - src/pages/404.astro
  - public/favicon.svg
  - public/og-default.png
autonomous: true
requirements:
  - QUAL-02
  - QUAL-03
  - QUAL-04
user_setup: []
must_haves:
  truths:
    - All Phase 1 pages share one layout that emits a non-empty `<title>`, `meta name="description"`, `link rel="canonical"`, `meta property="og:title"`, `meta property="og:description"`, `meta property="og:image"`, and favicon link from one centralized metadata API.
    - The homepage and 404 page build into real static HTML with a visible skip link, readable line length and spacing, obvious link treatment, and `:focus-visible` keyboard focus states.
    - The 404 page is a real static page with `noindex` metadata and a base-aware route back to the homepage.
  artifacts:
    - path: src/components/layout/BaseLayout.astro
      provides: Shared shell, metadata defaults, and accessibility landmarks
      min_lines: 40
    - path: src/components/layout/BaseLayout.astro
      provides: Page title wiring
      contains: "<title>"
    - path: src/components/layout/BaseLayout.astro
      provides: Meta description wiring
      contains: 'name="description"'
    - path: src/components/layout/BaseLayout.astro
      provides: Canonical URL wiring
      contains: 'rel="canonical"'
    - path: src/components/layout/BaseLayout.astro
      provides: Open Graph title wiring
      contains: 'property="og:title"'
    - path: src/components/layout/BaseLayout.astro
      provides: Open Graph description wiring
      contains: 'property="og:description"'
    - path: src/components/layout/BaseLayout.astro
      provides: Open Graph image wiring
      contains: 'property="og:image"'
    - path: src/components/layout/BaseLayout.astro
      provides: Favicon link wiring
      contains: 'rel="icon"'
    - path: src/styles/global.css
      provides: Readable typography, layout width, spacing, contrast, and focus styles
      min_lines: 60
    - path: src/styles/global.css
      provides: Skip-link styling for keyboard users
      contains: ".skip-link"
    - path: src/styles/global.css
      provides: Visible keyboard focus treatment
      contains: ":focus-visible"
    - path: src/styles/global.css
      provides: Readable text measure
      contains: "max-width"
    - path: src/pages/index.astro
      provides: Intentionally sparse homepage that exercises the shared shell
      min_lines: 20
      contains: "BaseLayout"
    - path: src/pages/404.astro
      provides: Static 404 page with recovery link and noindex behavior
      min_lines: 20
      contains: "noindex"
    - path: src/pages/404.astro
      provides: Base-aware route back to the homepage
      contains: "homePath"
    - path: public/favicon.svg
      provides: Shared favicon asset for the site shell
    - path: public/og-default.png
      provides: Default Open Graph image for share previews
  key_links:
    - from: src/components/layout/BaseLayout.astro
      to: src/data/site.ts
      via: Reads default title, description, and OG image from centralized site config
      pattern: "siteConfig"
    - from: src/pages/index.astro
      to: src/components/layout/BaseLayout.astro
      via: Wraps the homepage in the shared accessible shell
      pattern: "BaseLayout"
    - from: src/pages/404.astro
      to: src/components/layout/BaseLayout.astro
      via: Reuses the same metadata-aware layout for the error page
      pattern: "BaseLayout"
    - from: src/pages/404.astro
      to: src/lib/paths.ts
      via: Uses a base-aware helper for the link back home
      pattern: "homePath"
    - from: src/components/layout/BaseLayout.astro
      to: src/lib/paths.ts
      via: Uses shared helpers for canonical and asset URLs in metadata
      pattern: "canonicalUrl|assetPath"
---

<objective>
Build the accessible, readable Phase 1 site shell and the minimal pages needed to prove it.

Purpose: Establish the shared layout, typography baseline, metadata API, and sparse page set that later content phases can extend without redesigning the shell.
Output: A reusable `BaseLayout`, global CSS, a minimal homepage, a static `404` page, and shared favicon/OG assets.
</objective>

<execution_context>
@/Users/jstepek/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jstepek/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-publishing-foundation/01-RESEARCH.md
@.planning/phases/01-publishing-foundation/01-VALIDATION.md
@astro.config.mjs
@src/data/site.ts
@src/lib/paths.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a shared layout with accessible shell and centralized metadata</name>
  <files>src/components/layout/BaseLayout.astro, src/styles/global.css</files>
  <action>Build a `BaseLayout` that owns the document shell and accepts `title`, `description`, `canonicalPath`, `ogImage`, and `noindex`. Set document language, add `header`, `main`, and `footer` landmarks, and include a visible skip link. Create global CSS tuned for text-forward reading: system font stack, comfortable font sizing and line height, readable max width, strong contrast, touch-friendly spacing, link treatment that does not rely on color alone, and visible keyboard focus states. Do not optimize only for a tiny hero; the shell must still work for longer domain pages in later phases.</action>
  <verify>`pnpm astro check` passes, and local preview shows a visible skip link plus keyboard focus treatment.</verify>
  <done>The shared layout controls metadata centrally and provides a readable accessible baseline for future pages.</done>
</task>

<task type="auto">
  <name>Task 2: Replace scaffold content with the minimal homepage and static 404 page</name>
  <files>src/pages/index.astro, src/pages/404.astro</files>
  <action>Replace any placeholder scaffold content with an intentionally sparse homepage that is only rich enough to verify semantics, typography, and metadata flow. Add a real `404.astro` that uses the same layout, sets `noindex`, and includes a clear link back to the homepage through `homePath`. Keep copy minimal and do not pull final homepage positioning, domain navigation, or flagship content from later phases into this plan.</action>
  <verify>`pnpm astro build` emits both `dist/index.html` and `dist/404.html`.</verify>
  <done>The Phase 1 shell renders real pages with correct heading order, base-aware links, and a generated static 404 route.</done>
</task>

<task type="auto">
  <name>Task 3: Add shared favicon and default OG assets through the layout defaults</name>
  <files>public/favicon.svg, public/og-default.png</files>
  <action>Create a simple favicon and a default Open Graph image that match the minimal aesthetic, then wire them through the centralized metadata flow instead of page-by-page references. Keep the assets deliberately simple and dependable, and ensure the layout references them through shared helpers so they remain valid under a non-root base path.</action>
  <verify>A production build includes the favicon and OG asset in the output, and page source references the shared defaults rather than page-specific hard-coded values.</verify>
  <done>The site shell has favicon and social-preview defaults ready for later pages without duplicated metadata wiring.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm astro check` succeeds
- [ ] `pnpm astro build` creates `dist/index.html` and `dist/404.html`
- [ ] Built homepage and 404 output expose title, description, canonical URL, Open Graph tags, favicon wiring, skip-link/focus treatment, and `404` `noindex` behavior through the shared layout
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The Phase 1 shell is readable, accessible, metadata-aware, and still intentionally sparse enough to avoid pulling Phase 2 or 3 content forward
- Metadata and `404` behavior are wired centrally in the shared shell rather than duplicated page by page
</success_criteria>

<output>
After completion, create `.planning/phases/01-publishing-foundation/01-02-SUMMARY.md`
</output>
