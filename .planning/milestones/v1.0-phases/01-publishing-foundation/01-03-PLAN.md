---
phase: 01-publishing-foundation
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - package.json
  - scripts/validate-phase1.mjs
  - .github/workflows/deploy.yml
autonomous: true
requirements:
  - QUAL-01
  - QUAL-04
user_setup:
  - service: github
    why: Live GitHub Pages deployment and the phase-gate manual QA in `01-VALIDATION.md` require a real GitHub repository and Pages permissions that code changes alone cannot guarantee.
    dashboard_config:
      - task: Reconnect this repository to its GitHub remote or create the final GitHub repository before the first deploy run
        location: Local git remote plus GitHub repository
        details: The current project state does not expose a trustworthy Pages URL, so live base-path verification needs a real origin and publish target.
      - task: Confirm GitHub Pages publishes from GitHub Actions for this repository
        location: GitHub repository -> Settings -> Pages
        details: Use GitHub Actions as the deployment source so the workflow-created artifact can publish.
must_haves:
  truths:
    - `pnpm validate:phase1` fails if the built homepage is missing a non-empty `<title>`, `meta name="description"`, `link rel="canonical"`, `meta property="og:title"`, `meta property="og:description"`, `meta property="og:image"`, or favicon link.
    - `pnpm validate:phase1` fails if `dist/404.html` is missing, lacks `noindex`, or lacks a route back to the homepage.
    - GitHub Actions runs `pnpm astro check`, `pnpm astro build`, and `pnpm validate:phase1` before uploading and deploying the Pages artifact.
    - Live GitHub Pages verification remains an explicit phase-gate handoff through `user_setup` plus `01-VALIDATION.md`, rather than a hidden blocking step inside this autonomous plan.
  artifacts:
    - path: scripts/validate-phase1.mjs
      provides: Production artifact and metadata assertions for Phase 1
      min_lines: 30
    - path: scripts/validate-phase1.mjs
      provides: Homepage artifact assertion
      contains: "dist/index.html"
    - path: scripts/validate-phase1.mjs
      provides: Page title assertion
      contains: "<title>"
    - path: scripts/validate-phase1.mjs
      provides: Meta description assertion
      contains: 'name="description"'
    - path: scripts/validate-phase1.mjs
      provides: Canonical URL assertion
      contains: 'rel="canonical"'
    - path: scripts/validate-phase1.mjs
      provides: Open Graph title assertion
      contains: 'property="og:title"'
    - path: scripts/validate-phase1.mjs
      provides: Open Graph description assertion
      contains: 'property="og:description"'
    - path: scripts/validate-phase1.mjs
      provides: Open Graph image assertion
      contains: 'property="og:image"'
    - path: scripts/validate-phase1.mjs
      provides: Favicon assertion
      contains: 'rel="icon"'
    - path: scripts/validate-phase1.mjs
      provides: 404 artifact assertion
      contains: "dist/404.html"
    - path: scripts/validate-phase1.mjs
      provides: 404 noindex assertion
      contains: "noindex"
    - path: package.json
      provides: A `validate:phase1` script wired into the local toolchain
      contains: '"validate:phase1"'
    - path: .github/workflows/deploy.yml
      provides: GitHub Pages deployment workflow with build and validation gates
      contains: "deploy-pages"
    - path: .github/workflows/deploy.yml
      provides: Workflow invokes the validation gate before deploy
      contains: "pnpm validate:phase1"
  key_links:
    - from: .github/workflows/deploy.yml
      to: package.json
      via: Runs the local check, build, and validation scripts in CI before publish
      pattern: "pnpm astro check|pnpm astro build|pnpm validate:phase1"
    - from: .github/workflows/deploy.yml
      to: scripts/validate-phase1.mjs
      via: Invokes the HTML artifact assertions through the package script before deployment
      pattern: "validate:phase1"
    - from: scripts/validate-phase1.mjs
      to: dist/index.html
      via: Parses the built homepage artifact for title, description, canonical, OG, and favicon assertions
      pattern: "index.html"
    - from: scripts/validate-phase1.mjs
      to: dist/404.html
      via: Parses the built 404 artifact for output existence, noindex, and recovery-link assertions
      pattern: "404.html"
---

<objective>
Add repeatable validation and GitHub Pages deployment automation so Phase 1 exits with a trustworthy release gate.

Purpose: Turn the static shell into a deployable baseline by checking production artifacts before publish and wiring the same checks into GitHub Pages automation.
Output: A `validate:phase1` build-audit script and a GitHub Actions workflow that checks, builds, validates, and deploys the site once the repository is connected to GitHub Pages.
</objective>

<execution_context>
@/Users/jstepek/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jstepek/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-publishing-foundation/01-RESEARCH.md
@.planning/phases/01-publishing-foundation/01-VALIDATION.md
@package.json
@astro.config.mjs
@src/components/layout/BaseLayout.astro
@src/pages/index.astro
@src/pages/404.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a fast Phase 1 production-artifact validation script</name>
  <files>scripts/validate-phase1.mjs, package.json</files>
  <action>Implement a Node-based validation script that runs after `astro build` and asserts the required Phase 1 outputs exist: `dist/index.html`, `dist/404.html`, favicon asset, default OG asset, non-empty title, meta description, canonical URL, Open Graph tags, and `noindex` on the 404 page. Wire it to `pnpm validate:phase1`, keep it fast enough to run after each wave, and fail clearly when a required artifact or tag is missing.</action>
  <verify>`pnpm astro check && pnpm astro build && pnpm validate:phase1` passes locally.</verify>
  <done>The repo has a single repeatable command that catches missing homepage metadata, favicon wiring, and `404` behavior before deployment.</done>
</task>

<task type="auto">
  <name>Task 2: Add a GitHub Pages workflow that mirrors the local validation gate</name>
  <files>.github/workflows/deploy.yml</files>
  <action>Create a GitHub Actions Pages workflow that installs dependencies, runs `pnpm astro check`, runs the production build, runs `pnpm validate:phase1`, uploads the built `dist` artifact, and deploys with the official Pages actions. Configure permissions and branch triggers for a standard default-branch publish, and do not assume a custom domain or root-hosted deployment.</action>
  <verify>The workflow YAML is valid, references the correct build directory, and mirrors `pnpm astro check`, `pnpm astro build`, `pnpm validate:phase1`, `upload-pages-artifact`, and `deploy-pages` in the correct order.</verify>
  <done>GitHub Actions can publish the static build only after the same Phase 1 checks that pass locally.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm astro check && pnpm astro build && pnpm validate:phase1` succeeds
- [ ] `.github/workflows/deploy.yml` runs the same check, build, and validate sequence before `upload-pages-artifact` / `deploy-pages`
- [ ] `user_setup` plus `01-VALIDATION.md` clearly define the remaining live GitHub Pages deployment and manual QA handoff required before phase sign-off
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 has a repeatable local and CI release gate, and the remaining live Pages verification step is explicitly handed off through `user_setup` and `01-VALIDATION.md` instead of silently blocking an autonomous plan
</success_criteria>

<output>
After completion, create `.planning/phases/01-publishing-foundation/01-03-SUMMARY.md`
</output>
