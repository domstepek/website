---
phase: 01-publishing-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - astro.config.mjs
  - tsconfig.json
  - src/env.d.ts
  - src/data/site.ts
  - src/lib/paths.ts
  - src/pages/index.astro
autonomous: true
requirements:
  - QUAL-01
  - QUAL-04
user_setup: []
must_haves:
  truths:
    - The project boots as a pure static Astro site with explicit GitHub Pages-safe site and base configuration instead of root-only assumptions.
    - Shared site constants and path helpers exist before page templates so later phases can generate canonical URLs, asset URLs, and internal links from one place.
  artifacts:
    - path: package.json
      provides: Astro and project scripts for local checks and production build
      contains: '"build"'
    - path: astro.config.mjs
      provides: Static Astro configuration with explicit site and base handling
      contains: "base:"
    - path: src/data/site.ts
      provides: Centralized site metadata and deploy-path configuration
      exports:
        - siteConfig
    - path: src/lib/paths.ts
      provides: Base-aware helpers for internal routes, assets, and canonical URLs
      exports:
        - homePath
        - assetPath
        - canonicalUrl
  key_links:
    - from: src/lib/paths.ts
      to: src/data/site.ts
      via: Imports shared site configuration for URL construction
      pattern: "siteConfig"
---

<objective>
Bootstrap the Phase 1 Astro foundation with deployment-safe configuration and shared path primitives.

Purpose: Remove the highest-risk GitHub Pages failure mode early by making `site`, `base`, canonical URLs, and asset/internal links configurable before the shell and metadata are layered on top.
Output: A minimal Astro/TypeScript project scaffold plus centralized site config and base-aware path helpers that later plans can build against.
</objective>

<execution_context>
@/Users/jstepek/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jstepek/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-publishing-foundation/01-RESEARCH.md
@.planning/phases/01-publishing-foundation/01-VALIDATION.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap a minimal Astro + TypeScript workspace</name>
  <files>package.json, pnpm-lock.yaml, tsconfig.json, src/env.d.ts, src/pages/index.astro</files>
  <action>Initialize a plain Astro project with `pnpm`, keep it static by default, and strip any starter-kit demo content back to the smallest possible placeholder page that still proves the toolchain works. Add standard scripts for `dev`, `check`, and `build`, but do not introduce a client framework, content collections, domain routes, or later-phase homepage content in this foundation pass.</action>
  <verify>`pnpm astro check` and `pnpm astro build` both succeed from a clean install.</verify>
  <done>The repository has a working Astro toolchain, the starter scaffolding is cleaned up, and the repo is ready for shared layout work.</done>
</task>

<task type="auto">
  <name>Task 2: Lock Astro config to GitHub Pages-safe static output</name>
  <files>astro.config.mjs</files>
  <action>Configure Astro for static output with explicit `site` and `base` handling suitable for GitHub Pages project-site deployment, and choose one URL strategy up front: directory-style output with trailing slashes. Keep the public URL and base path configurable so a repo rename or custom domain later does not force route rewrites, and avoid any assumptions that the site will live at `/`.</action>
  <verify>`pnpm astro build` emits a `dist/` directory without config errors, and the config clearly defines static-export deployment behavior.</verify>
  <done>`astro.config.mjs` is ready for Pages deployment and future deep routes without root-relative coupling.</done>
</task>

<task type="auto">
  <name>Task 3: Centralize site constants and base-aware path helpers</name>
  <files>src/data/site.ts, src/lib/paths.ts</files>
  <action>Create a single `siteConfig` module for site name, default title and description, site URL, base path, and default social asset values. Add a helper module that exports `homePath`, `assetPath`, `canonicalUrl`, and any minimal future-safe route builders needed for later phases. Avoid raw root-relative strings like `/favicon.svg` or `/domains/...` so every later page inherits base-aware behavior automatically.</action>
  <verify>`pnpm astro check` passes after the helpers are imported into the scaffolded page or a small smoke usage.</verify>
  <done>Later plans can import shared configuration and URL helpers instead of duplicating metadata or path logic.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm astro check` succeeds
- [ ] `pnpm astro build` succeeds
- [ ] `site`, `base`, and shared URL helpers are implemented without hard-coded root-relative assumptions
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo has a buildable static Astro baseline that future plans can extend without reworking deploy-path logic
</success_criteria>

<output>
After completion, create `.planning/phases/01-publishing-foundation/01-01-SUMMARY.md`
</output>
