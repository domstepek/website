---
phase: 04-flagship-proof-visuals
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - scripts/validate-phase4.mjs
  - package.json
autonomous: true
requirements:
  - HIGH-01
  - HIGH-02
  - HIGH-03
  - HIGH-04
user_setup: []
must_haves:
  truths:
    - `pnpm validate:phase4` inspects built `dist/domains/<slug>/index.html` artifacts and fails if a domain page is missing the flagship section, has fewer than 1 or more than 2 flagship entries, or is missing the required per-flagship structural markers for title, problem, role, constraints, decisions, outcomes, or stack.
    - The Phase 4 validator treats visuals as optional but strict when present: any `data-flagship-visual` must emit an `img` with non-empty `src` and `alt`, and local highlight assets must resolve through base-aware site paths.
    - `pnpm validate:site` expands through Phase 4 without changing the existing GitHub Pages workflow file because CI already runs the aggregate site gate after build.
  artifacts:
    - path: scripts/validate-phase4.mjs
      provides: Dist-first validator for flagship structure across all five domain pages
    - path: scripts/validate-phase4.mjs
      provides: Flagship-section presence and count assertions
      contains: "data-flagship-highlights"
    - path: scripts/validate-phase4.mjs
      provides: Required flagship field assertions
      contains: "data-flagship-problem"
    - path: scripts/validate-phase4.mjs
      provides: Optional visual assertions
      contains: "data-flagship-visual"
    - path: package.json
      provides: Dedicated Phase 4 validation script
      contains: "\"validate:phase4\""
    - path: package.json
      provides: Aggregate site validation widened through Phase 4
      contains: "\"validate:site\""
  key_links:
    - from: scripts/validate-phase4.mjs
      to: dist/domains/analytics/index.html
      via: Parses built domain artifacts instead of source TypeScript modules
      pattern: "analytics|infrastructure|ai-ml|product|developer-experience"
    - from: scripts/validate-phase4.mjs
      to: src/components/domains/DomainPage.astro
      via: Reads the stable flagship `data-*` markers emitted by the shared domain template
      pattern: "data-flagship-highlights|data-flagship|data-flagship-problem|data-flagship-stack|data-flagship-visual"
    - from: package.json
      to: scripts/validate-phase1.mjs
      via: Keeps earlier phase validators in the aggregate site gate while adding Phase 4
      pattern: "validate:phase1|validate:phase2|validate:phase3|validate:phase4"
    - from: .github/workflows/deploy.yml
      to: package.json
      via: Existing CI already runs `pnpm validate:site`, so widening the package script automatically extends the deploy gate
      pattern: "pnpm validate:site"
---

<objective>
Add a dist-first Phase 4 validator and extend the shared site release gate through the flagship work.

Purpose: Make the new flagship sections trustworthy by validating the built artifacts that GitHub Pages will publish, not just the source modules that generated them.
Output: A `validate:phase4` script plus an updated `validate:site` command that catches missing or structurally weak flagship proof before deploy.
</objective>

<execution_context>
@/Users/jstepek/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jstepek/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-flagship-proof-visuals/04-RESEARCH.md
@.planning/phases/04-flagship-proof-visuals/04-VALIDATION.md
@package.json
@.github/workflows/deploy.yml
@scripts/validate-phase1.mjs
@scripts/validate-phase2.mjs
@scripts/validate-phase3.mjs
@src/components/domains/DomainPage.astro
@src/lib/paths.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement a fast dist-first validator for flagship structure</name>
  <files>scripts/validate-phase4.mjs</files>
  <action>Create a Node-based validator that runs after `astro build` and inspects each built domain artifact in `dist/domains/<slug>/index.html`. Assert that every domain page still exists, exposes one `data-flagship-highlights` section, renders at least 1 and at most 2 `data-flagship` entries, and gives each flagship non-empty title, problem, role, and outcomes text plus at least one constraint item, one decision item, and one stack item. Validate any `data-flagship-proof-link` structurally as absolute `http(s)` URLs. If a `data-flagship-visual` exists, require an `img` with non-empty `src` and `alt`, and confirm local highlight asset paths are base-aware. Do not read source TS modules and do not fail when a flagship intentionally has no visual.</action>
  <verify>`pnpm astro check && pnpm astro build && node ./scripts/validate-phase4.mjs` passes locally.</verify>
  <done>The repo has a Phase 4 artifact audit that proves the flagship sections exist in emitted HTML and meet the required structural depth.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 4 validation to the aggregate site gate</name>
  <files>package.json</files>
  <action>Add `validate:phase4` and extend `validate:site` so the existing Phase 1 through Phase 3 validators still run and the new Phase 4 validator is included in the same aggregate command. Keep the script chain simple so the already-wired GitHub Pages workflow automatically inherits the stronger gate without a separate CI path or workflow rewrite.</action>
  <verify>`pnpm astro check && pnpm astro build && pnpm validate:site` passes locally.</verify>
  <done>Local and CI validation now cover the site through the end of Phase 4 through one shared release gate.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm astro check && pnpm astro build && pnpm validate:site` succeeds
- [ ] `pnpm validate:phase4` inspects built `dist` HTML and fails on missing flagship sections, wrong flagship counts, missing title or required story fields, missing constraint or decision items, or missing stack content
- [ ] Proof-link validation is structural only: require absolute `http(s)` URLs, but do not depend on remote availability
- [ ] Visual validation is conditional: allow no visual, but when a visual exists require non-empty `src` and `alt` plus a base-aware local path shape
- [ ] The existing GitHub Pages workflow inherits the new gate automatically through `pnpm validate:site`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 4 exits with a repeatable local and CI artifact gate that proves the flagship sections as built output instead of trusting source content alone
</success_criteria>

<output>
After completion, create `.planning/phases/04-flagship-proof-visuals/04-03-SUMMARY.md`
</output>
