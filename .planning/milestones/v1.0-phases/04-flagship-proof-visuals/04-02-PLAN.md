---
phase: 04-flagship-proof-visuals
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/data/domains/analytics.ts
  - src/data/domains/infrastructure.ts
  - src/data/domains/ai-ml.ts
  - src/data/domains/product.ts
  - src/data/domains/developer-experience.ts
  - src/components/domains/DomainPage.astro
  - src/styles/global.css
  - public/highlights/**/*
autonomous: true
requirements:
  - HIGH-01
  - HIGH-02
  - HIGH-03
  - HIGH-04
user_setup: []
must_haves:
  truths:
    - Every domain page ends up with 1 to 2 real flagship highlights, and the flagship section stays clearly distinct from the lighter `supporting work` section instead of flattening the site into a generic project gallery.
    - Each flagship tells the same scannable story shape: summary, problem, role, constraints, decisions, outcomes, stack, proof links, and an optional visual only when a screenshot or diagram materially improves understanding.
    - Visual assets, when used, live under `public/highlights/<domain>/<flagship>/...` and resolve through the existing base-aware asset flow instead of remote fetches, `src/assets`, or a JS gallery system.
    - The domain pages remain text-forward and readable on mobile and desktop even after the longest flagship titles, lists, proof links, and any figures are present.
  artifacts:
    - path: src/data/domains/analytics.ts
      provides: Final analytics flagship content aligned with the shared Phase 4 schema
      contains: "flagships"
    - path: src/data/domains/infrastructure.ts
      provides: Real flagship content for the infrastructure domain
      contains: "flagships"
    - path: src/data/domains/ai-ml.ts
      provides: Real flagship content for the ai / ml domain
      contains: "flagships"
    - path: src/data/domains/product.ts
      provides: Real flagship content for the product domain
      contains: "flagships"
    - path: src/data/domains/developer-experience.ts
      provides: Real flagship content for the developer experience domain
      contains: "flagships"
    - path: src/components/domains/DomainPage.astro
      provides: Optional visual rendering and final flagship section structure
      contains: "data-flagship-visual"
    - path: src/styles/global.css
      provides: Shared spacing, typography, and figure treatment for flagship sections
      contains: ".flagship"
    - path: public/highlights/**/*
      provides: Optional local screenshot or diagram assets for flagship stories
  key_links:
    - from: src/data/domains/analytics.ts
      to: src/components/domains/DomainPage.astro
      via: Supplies the pilot plus final flagship entries through the shared page pattern
      pattern: "flagships"
    - from: src/data/domains/infrastructure.ts
      to: src/components/domains/DomainPage.astro
      via: Adds infrastructure flagship proof without introducing a second template
      pattern: "flagships"
    - from: src/data/domains/ai-ml.ts
      to: src/components/domains/DomainPage.astro
      via: Adds ai / ml flagship proof through the shared rendering pattern
      pattern: "flagships"
    - from: src/data/domains/product.ts
      to: src/components/domains/DomainPage.astro
      via: Adds product flagship proof through the shared rendering pattern
      pattern: "flagships"
    - from: src/data/domains/developer-experience.ts
      to: src/components/domains/DomainPage.astro
      via: Adds developer experience flagship proof through the shared rendering pattern
      pattern: "flagships"
    - from: src/components/domains/DomainPage.astro
      to: src/lib/paths.ts
      via: Uses `assetPath()` so any local flagship visuals stay base-path aware in built output
      pattern: "assetPath"
    - from: src/components/domains/DomainPage.astro
      to: src/styles/global.css
      via: Uses shared flagship class hooks for section rhythm, lists, figures, and proof-link spacing
      pattern: "flagship"
---

<objective>
Fill the remaining domain pages with real flagship proof, add purposeful visuals where they help, and finish the shared flagship presentation.

Purpose: Multiply the proven pilot pattern across all five domains without widening scope into standalone routes, a project gallery, or Phase 5 personal-context work.
Output: Complete flagship content across the domain modules, optional local visuals where they materially improve understanding, and shared template or CSS polish tuned to the full content set.
</objective>

<execution_context>
@/Users/jstepek/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jstepek/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-flagship-proof-visuals/04-RESEARCH.md
@.planning/phases/04-flagship-proof-visuals/04-VALIDATION.md
@src/data/domains/analytics.ts
@src/data/domains/infrastructure.ts
@src/data/domains/ai-ml.ts
@src/data/domains/product.ts
@src/data/domains/developer-experience.ts
@src/components/domains/DomainPage.astro
@src/styles/global.css
@src/lib/paths.ts
@public/favicon.svg
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand all five domain modules to 1 to 2 real flagship stories each</name>
  <files>src/data/domains/analytics.ts, src/data/domains/infrastructure.ts, src/data/domains/ai-ml.ts, src/data/domains/product.ts, src/data/domains/developer-experience.ts</files>
  <action>Revisit the pilot analytics module and fill the remaining four domain modules with 1 to 2 flagship highlights each, promoting from the current supporting-work shortlist before inventing new domain ownership. Every flagship must include summary, problem, role, constraints, decisions, outcomes, stack, and proof links. Keep the content compact and concrete, trim or reposition any supporting-work entries that would otherwise duplicate a flagship verbatim, and preserve the Phase 2 domain boundaries instead of reopening cross-domain ownership from scratch.</action>
  <verify>`pnpm astro check && pnpm astro build` succeed and each built domain page renders at least one flagship entry before the `supporting work` section.</verify>
  <done>All five domains now have real flagship proof rather than a single pilot or placeholder structure.</done>
</task>

<task type="auto">
  <name>Task 2: Add visuals only where they materially improve understanding</name>
  <files>src/data/domains/analytics.ts, src/data/domains/infrastructure.ts, src/data/domains/ai-ml.ts, src/data/domains/product.ts, src/data/domains/developer-experience.ts, public/highlights/**/*</files>
  <action>Add local visuals only for the subset of flagships where a screenshot or diagram genuinely helps the visitor understand the work faster. Store every local asset under `public/highlights/<domain>/<flagship>/...`, wire the corresponding `visual` objects in the domain modules, require real alt text and short explanatory captions, and prefer truthful screenshots or simple static diagrams from public, demo, or reproducible artifacts. If no trustworthy visual is available for a flagship, leave `visual` unset rather than adding decorative media.</action>
  <verify>`pnpm astro build` succeeds and any domain page that renders a flagship visual emits a non-empty local `img src` plus non-empty `alt` text in built HTML.</verify>
  <done>The site has purposeful visuals where they help, without turning the flagship section into a gallery or asset-management project.</done>
</task>

<task type="auto">
  <name>Task 3: Finish the shared flagship layout and readability pass</name>
  <files>src/components/domains/DomainPage.astro, src/styles/global.css</files>
  <action>Adjust the shared template and global styles so flagship highlights stay easy to scan with real content: clear section separation from supporting work, comfortable list rhythm for constraints, decisions, outcomes, and stack, figure spacing that keeps text primary, and mobile or desktop behavior that handles the longest title and deepest section gracefully. Keep the presentation text-forward, preserve the existing site shell and focus states, and do not add cards, tabs, carousels, or bespoke per-domain layouts.</action>
  <verify>`pnpm astro check && pnpm astro build` succeed and the longest flagship pages render without overflowing, broken spacing, or missing focus styles.</verify>
  <done>The full Phase 4 content set fits the site's minimal reading experience instead of feeling bolted on.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm astro check && pnpm astro build` succeeds
- [ ] Each domain page renders 1 to 2 flagship highlights before `supporting work`
- [ ] Every flagship exposes visible problem, role, constraints, decisions, outcomes, and stack content
- [ ] Any included visuals resolve through local base-aware asset paths under `public/highlights/...` and include meaningful alt text
- [ ] Flagship treatment stays text-first and clearly distinct from supporting work on mobile and desktop widths
- [ ] No content collections, CMS work, or standalone flagship routes were introduced
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- All five domain pages now carry convincing flagship proof and optional visuals without breaking the minimal domain-first architecture
</success_criteria>

<output>
After completion, create `.planning/phases/04-flagship-proof-visuals/04-02-SUMMARY.md`
</output>
