---
phase: 04-flagship-proof-visuals
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/domains/types.ts
  - src/data/domains/analytics.ts
  - src/components/domains/DomainPage.astro
autonomous: true
requirements:
  - HIGH-01
  - HIGH-02
  - HIGH-03
user_setup: []
must_haves:
  truths:
    - The repo keeps the existing typed domain-data modules and shared `DomainPage.astro` pattern, adding flagship data inline on `DomainEntry` instead of migrating to content collections, a CMS, or standalone case-study routes.
    - The shared domain page renders a new `flagship highlights` section between the Phase 2 boundary content and `supporting work`, using one repeated scannable structure for title, summary, problem, role, constraints, decisions, outcomes, stack, proof links, and optional visual support.
    - One real pilot domain proves the flagship pattern end to end before the rest of the domains are expanded, so later plans can multiply a working structure instead of guessing on five pages at once.
  artifacts:
    - path: src/data/domains/types.ts
      provides: Structured flagship schema layered onto the existing domain registry
      exports:
        - FlagshipVisual
        - FlagshipHighlight
        - DomainEntry
    - path: src/data/domains/types.ts
      provides: Optional per-flagship visual support without requiring a visual on every story
      contains: "visual?"
    - path: src/data/domains/analytics.ts
      provides: Pilot analytics flagships promoted from existing supporting-work candidates
      contains: "flagships"
    - path: src/components/domains/DomainPage.astro
      provides: Shared flagship-highlights rendering inside the existing domain-page template
      contains: "data-flagship-highlights"
    - path: src/components/domains/DomainPage.astro
      provides: Stable per-flagship structural markers for later dist validation
      contains: "data-flagship-problem"
    - path: src/components/domains/DomainPage.astro
      provides: Stable stack and proof-link markers for flagship entries
      contains: "data-flagship-stack"
  key_links:
    - from: src/components/domains/DomainPage.astro
      to: src/data/domains/types.ts
      via: Renders the shared flagship structure directly from the typed domain entry contract
      pattern: "flagships|FlagshipHighlight"
    - from: src/components/domains/DomainPage.astro
      to: src/lib/paths.ts
      via: Keeps home navigation and any optional flagship visuals base-path aware through shared path helpers
      pattern: "homePath|domainPath|assetPath"
    - from: src/pages/domains/[slug].astro
      to: src/components/domains/DomainPage.astro
      via: Continues to render all domain routes through one shared template instead of introducing new highlight routes
      pattern: "DomainPage"
---

<objective>
Introduce the shared flagship data model and prove it with one real domain inside the existing domain-page pattern.

Purpose: Establish the minimum Phase 4 architecture without reopening the site structure, moving content out of TypeScript modules, or turning v1 into standalone case studies.
Output: A typed flagship schema, shared flagship rendering in `DomainPage.astro`, and one pilot domain that shows the final storytelling structure end to end.
</objective>

<execution_context>
@/Users/jstepek/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jstepek/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-flagship-proof-visuals/04-RESEARCH.md
@.planning/phases/04-flagship-proof-visuals/04-VALIDATION.md
@src/data/domains/types.ts
@src/data/domains/analytics.ts
@src/data/domains/index.ts
@src/components/domains/DomainPage.astro
@src/pages/domains/[slug].astro
@src/lib/paths.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the typed domain schema for structured flagship highlights</name>
  <files>src/data/domains/types.ts</files>
  <action>Add explicit `FlagshipVisual` and `FlagshipHighlight` types to the existing domain model, then add a `flagships` array to `DomainEntry`. Keep the field shape scannable and validator-friendly: `slug`, `title`, `summary`, `problem`, `role`, `constraints`, `decisions`, `outcomes`, `stack`, optional `proofLinks`, and optional `visual`. Reuse the existing `ProofLink` type, keep the current `summary` and `supportingWork` contract intact for the homepage and domain routes, and do not migrate content into a new authoring system.</action>
  <verify>`pnpm astro check` passes after the shared domain types compile cleanly.</verify>
  <done>The repo has one typed flagship contract that all five domain modules can adopt without changing the route architecture.</done>
</task>

<task type="auto">
  <name>Task 2: Add the shared flagship section to the existing domain-page template</name>
  <files>src/components/domains/DomainPage.astro</files>
  <action>Extend `DomainPage.astro` so it renders `flagship highlights` after `what belongs here` and before `supporting work`. Use semantic section, article, list, and optional figure markup, and emit stable `data-*` markers for the flagship section, each flagship entry, title, problem, role, constraints, decisions, outcomes, stack, proof links, and optional visual. Use the existing `assetPath()` helper for any local visual `src` values, but do not introduce a gallery, lightbox, tabs, or any new routes.</action>
  <verify>`pnpm astro check && pnpm astro build` succeed and the built domain route can render the new flagship section without layout or route errors.</verify>
  <done>The shared domain template can render Phase 4 flagship structure while preserving the existing Phase 2 page pattern.</done>
</task>

<task type="auto">
  <name>Task 3: Pilot the flagship structure with the analytics domain</name>
  <files>src/data/domains/analytics.ts</files>
  <action>Promote 1 to 2 analytics stories from the current supporting-work shortlist into real flagships, starting with the strongest candidates such as `web portal` and one of `superset on stargazer` or `umami`. Write concise, grounded content for summary, problem, role, constraints, decisions, outcomes, stack, and proof links so the pilot proves real depth without turning into a long case-study essay. Keep the remaining `supportingWork` entries as lighter evidence, and only add a visual here if a truthful local asset already exists or is trivial to produce later without blocking the pilot.</action>
  <verify>`pnpm astro check && pnpm astro build` succeed and `dist/domains/analytics/index.html` clearly renders both the new flagship section and the existing supporting-work section.</verify>
  <done>One real domain now proves the flagship schema, template, and content shape end to end.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm astro check && pnpm astro build` succeeds
- [ ] `DomainEntry` still drives `src/pages/domains/[slug].astro` through the shared typed registry
- [ ] The analytics domain renders 1 to 2 flagship highlights above `supporting work`
- [ ] The shared template emits stable `data-flagship-*` markers for title, problem, role, constraints, decisions, outcomes, stack, and proof links
- [ ] No content collections, CMS work, homepage rewrites, or standalone flagship routes were introduced
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 4 exits this plan with one proven flagship pattern that can scale to the remaining domains without changing the repo's architecture
</success_criteria>

<output>
After completion, create `.planning/phases/04-flagship-proof-visuals/04-01-SUMMARY.md`
</output>
