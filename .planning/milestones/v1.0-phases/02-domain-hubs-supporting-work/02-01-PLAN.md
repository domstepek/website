---
phase: 02-domain-hubs-supporting-work
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/domains/types.ts
  - src/data/domains/index.ts
  - src/data/domains/analytics.ts
  - src/data/domains/infrastructure.ts
  - src/data/domains/ai-ml.ts
  - src/data/domains/product.ts
  - src/data/domains/developer-experience.ts
  - src/components/domains/DomainPage.astro
  - src/pages/domains/[slug].astro
autonomous: true
requirements:
  - DOMN-01
  - DOMN-02
  - DOMN-03
  - DOMN-04
user_setup: []
must_haves:
  truths:
    - The five v1 domains are locked to `/domains/[slug]/` using the slugs `analytics`, `infrastructure`, `ai-ml`, `product`, and `developer-experience`, and they are generated from one typed registry instead of five hand-written pages.
    - The route for each domain page reuses `BaseLayout`, passes the domain-specific `title` and `seoDescription` into the layout, and wires `canonicalPath` through `domainPath(slug)` rather than duplicating head tags or assuming root-relative URLs.
    - The shared domain page template renders an explicit top-of-page `back home` link, a short first-person thesis, short belonging bullets, an explicit scope line, a supporting-work section with rendered entries, and inline proof links without introducing flagship sections or a `/domains/` index page.
    - Stable validation markers such as `data-domain-page`, `data-domain-thesis`, `data-supporting-work`, `data-supporting-item`, `data-proof-link`, and `data-back-home` exist in the shared template so later artifact validation can inspect built HTML instead of source files.
  artifacts:
    - path: src/data/domains/types.ts
      provides: Typed Phase 2 content model for domain pages
      exports:
        - DomainSlug
        - ProofLink
        - SupportingWorkItem
        - DomainEntry
    - path: src/data/domains/index.ts
      provides: Ordered domain registry and slug lookup helpers
      exports:
        - domainSlugs
        - domains
        - getDomainBySlug
    - path: src/data/domains/analytics.ts
      provides: Baseline analytics domain content module
      contains: '"analytics"'
    - path: src/data/domains/infrastructure.ts
      provides: Baseline infrastructure domain content module
      contains: '"infrastructure"'
    - path: src/data/domains/ai-ml.ts
      provides: Baseline ai / ml domain content module
      contains: '"ai-ml"'
    - path: src/data/domains/product.ts
      provides: Baseline product domain content module
      contains: '"product"'
    - path: src/data/domains/developer-experience.ts
      provides: Baseline developer experience domain content module
      contains: '"developer-experience"'
    - path: src/components/domains/DomainPage.astro
      provides: Shared presentational template for every domain route
      min_lines: 60
    - path: src/components/domains/DomainPage.astro
      provides: Stable domain-page validation marker
      contains: "data-domain-page"
    - path: src/components/domains/DomainPage.astro
      provides: Stable thesis validation marker
      contains: "data-domain-thesis"
    - path: src/components/domains/DomainPage.astro
      provides: Stable supporting-work validation marker
      contains: "data-supporting-work"
    - path: src/components/domains/DomainPage.astro
      provides: Stable supporting-entry validation marker
      contains: "data-supporting-item"
    - path: src/components/domains/DomainPage.astro
      provides: Stable proof-link validation marker
      contains: "data-proof-link"
    - path: src/components/domains/DomainPage.astro
      provides: Explicit back-home link marker
      contains: "data-back-home"
    - path: src/pages/domains/[slug].astro
      provides: Dynamic static route generation for all five domain pages
      contains: "getStaticPaths"
    - path: src/pages/domains/[slug].astro
      provides: Page-level metadata wiring through the existing shared layout
      contains: "BaseLayout"
    - path: src/pages/domains/[slug].astro
      provides: Domain-specific title and description handoff to the shared layout
      contains: "seoDescription"
    - path: src/pages/domains/[slug].astro
      provides: Base-aware canonical path wiring for domain routes
      contains: "domainPath"
  key_links:
    - from: src/pages/domains/[slug].astro
      to: src/data/domains/index.ts
      via: Generates static paths and resolves page data from one ordered registry
      pattern: "domains|getDomainBySlug"
    - from: src/pages/domains/[slug].astro
      to: src/components/domains/DomainPage.astro
      via: Renders every domain through one shared text-first template
      pattern: "DomainPage"
    - from: src/pages/domains/[slug].astro
      to: src/lib/paths.ts
      via: Passes base-aware canonical paths into the shared layout for each domain route
      pattern: "domainPath"
    - from: src/components/domains/DomainPage.astro
      to: src/lib/paths.ts
      via: Uses shared route helpers for the explicit home return link and optional cross-domain mentions
      pattern: "homePath|domainPath"
---

<objective>
Bootstrap the repeatable domain-hub architecture with typed data, a shared template, and real routes for the five v1 domains.

Purpose: Lock the domain-first information architecture early so Phase 2 proves the `/domains/[slug]/` pattern before any homepage rewrite or flagship storytelling.
Output: A typed `src/data/domains` layer, one shared `DomainPage`, and a dynamic route that renders all five baseline domain pages through `BaseLayout`.
</objective>

<execution_context>
@/Users/jstepek/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jstepek/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-domain-hubs-supporting-work/02-CONTEXT.md
@.planning/phases/02-domain-hubs-supporting-work/02-RESEARCH.md
@.planning/phases/02-domain-hubs-supporting-work/02-VALIDATION.md
@package.json
@astro.config.mjs
@src/components/layout/BaseLayout.astro
@src/lib/paths.ts
@src/styles/global.css
@src/data/site.ts
@src/pages/index.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the Phase 2 domain schema, slug registry, and module layout</name>
  <files>src/data/domains/types.ts, src/data/domains/index.ts, src/data/domains/analytics.ts, src/data/domains/infrastructure.ts, src/data/domains/ai-ml.ts, src/data/domains/product.ts, src/data/domains/developer-experience.ts</files>
  <action>Create a small typed data layer under `src/data/domains/` that locks the five visitor-facing domains and the exact route slugs up front. Model fields for `title`, `summary`, `seoDescription`, `thesis`, `scope`, `belongsHere`, `supportingWork`, and optional `relatedDomains`, and define the supporting data shape explicitly: supporting items need `title`, `context`, and optional `proofLinks`, while proof links need `label` and `href`. Keep the source in TypeScript modules rather than Astro frontmatter or content collections. Seed each domain module with minimal valid structure so the route can consume it immediately in the next task, but do not pull homepage positioning or flagship-story schema into this plan.</action>
  <verify>`pnpm astro check` passes after the new typed modules compile cleanly.</verify>
  <done>Typed content scaffolding exists for all five domain hubs, and the phase no longer depends on ad-hoc per-page copy objects.</done>
</task>

<task type="auto">
  <name>Task 2: Build the shared `/domains/[slug]/` route and page template</name>
  <files>src/components/domains/DomainPage.astro, src/pages/domains/[slug].astro</files>
  <action>Add one shared `DomainPage.astro` component and one dynamic route file that uses `getStaticPaths`, `getDomainBySlug`, `BaseLayout`, `domainPath(slug)`, and `homePath`. The route must pass each domain entry's `title`, `seoDescription`, and canonical path into `BaseLayout`. The template must include the explicit top `back home` link, domain title, thesis block, short bullets, scope line, supporting-work section, rendered supporting entries, inline proof links, and stable `data-*` markers for later dist validation. Do not add a `/domains/` overview route, client-side interactivity, or a second layout shell.</action>
  <verify>`pnpm astro check && pnpm astro build` succeed and the build can generate directory-style domain pages from the shared registry without route or layout errors.</verify>
  <done>The repo has one reusable domain route/template instead of five hand-authored pages.</done>
</task>

<task type="auto">
  <name>Task 3: Replace seeded content with baseline v1 entries for all five domains</name>
  <files>src/data/domains/analytics.ts, src/data/domains/infrastructure.ts, src/data/domains/ai-ml.ts, src/data/domains/product.ts, src/data/domains/developer-experience.ts</files>
  <action>Fill each domain module with baseline copy good enough to make the routes real: one short first-person thesis sentence, 2 to 4 belonging bullets, one explicit scope line, and roughly 3 to 6 curated supporting-work items. Assign each project to one canonical home domain based on the primary problem solved, keep proof links inline with the supporting entry, allow zero proof links on some entries, but ensure every page has at least one outward proof link somewhere. Leave flagship sections, visuals, screenshots, and homepage navigation work for later phases.</action>
  <verify>`pnpm astro build` emits `dist/domains/analytics/index.html`, `dist/domains/infrastructure/index.html`, `dist/domains/ai-ml/index.html`, `dist/domains/product/index.html`, and `dist/domains/developer-experience/index.html`.</verify>
  <done>All five baseline domain routes exist and are ready for copy and presentation refinement instead of structural work.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm astro check` succeeds
- [ ] `pnpm astro build` creates all five `dist/domains/<slug>/index.html` artifacts
- [ ] Every domain route uses `BaseLayout` plus `domainPath(slug)` and `homePath` rather than duplicated metadata or raw root-relative links
- [ ] Built domain routes expose the explicit top `back home` link and inline proof-link pattern through the shared template
- [ ] No `/domains/` overview page, homepage rewrite, or flagship sections were pulled into this plan
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo has a reusable typed domain-hub architecture and five real Phase 2 routes without widening scope into Phase 3 or Phase 4
</success_criteria>

<output>
After completion, create `.planning/phases/02-domain-hubs-supporting-work/02-01-SUMMARY.md`
</output>
