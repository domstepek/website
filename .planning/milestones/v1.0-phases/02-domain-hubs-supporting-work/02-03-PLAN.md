---
phase: 02-domain-hubs-supporting-work
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - package.json
  - scripts/validate-phase2.mjs
  - .github/workflows/deploy.yml
autonomous: true
requirements:
  - DOMN-01
  - DOMN-02
  - DOMN-03
  - DOMN-04
user_setup: []
must_haves:
  truths:
    - `pnpm validate:phase2` fails if any of the five built domain artifacts are missing or if a built page is missing a non-empty `<title>`, canonical URL, explicit back-home marker with the base-aware home `href`, thesis marker, supporting-work marker, at least one rendered supporting-work entry, or any outward proof link.
    - Phase 2 validation inspects emitted `dist` HTML and only checks structural proof-link presence plus absolute `http(s)` URL shape; it does not depend on third-party site availability.
    - Local and CI release gates run Phase 1 and Phase 2 validation together after `astro build`, preferably through a single aggregate script such as `validate:site`.
  artifacts:
    - path: scripts/validate-phase2.mjs
      provides: Dist-first artifact assertions for all five domain pages
      min_lines: 60
    - path: scripts/validate-phase2.mjs
      provides: Domain artifact existence checks
      contains: "dist/domains/analytics/index.html"
    - path: scripts/validate-phase2.mjs
      provides: Shared template marker assertions
      contains: "data-domain-page"
    - path: scripts/validate-phase2.mjs
      provides: Supporting-entry count assertions
      contains: "data-supporting-item"
    - path: scripts/validate-phase2.mjs
      provides: Proof-link assertions
      contains: "data-proof-link"
    - path: package.json
      provides: Phase 2 validation script
      contains: '"validate:phase2"'
    - path: package.json
      provides: Aggregate site validation script for future phases
      contains: '"validate:site"'
    - path: .github/workflows/deploy.yml
      provides: Combined site validation gate before Pages upload
      contains: "pnpm validate:site"
    - path: .github/workflows/deploy.yml
      provides: Existing Pages artifact upload and deploy flow preserved
      contains: "upload-pages-artifact"
  key_links:
    - from: scripts/validate-phase2.mjs
      to: dist/domains/analytics/index.html
      via: Parses the built domain artifacts rather than source files
      pattern: "analytics|infrastructure|ai-ml|product|developer-experience"
    - from: scripts/validate-phase2.mjs
      to: src/components/domains/DomainPage.astro
      via: Reads the stable `data-*` markers emitted by the shared domain template in built HTML
      pattern: "data-domain-page|data-domain-thesis|data-supporting-work|data-proof-link|data-back-home"
    - from: package.json
      to: scripts/validate-phase1.mjs
      via: Aggregates Phase 1 and Phase 2 validators under one site-level command
      pattern: "validate:phase1|validate:phase2"
    - from: .github/workflows/deploy.yml
      to: package.json
      via: Runs the local check, build, and aggregate validation sequence in CI before Pages publish
      pattern: "pnpm astro check|pnpm astro build|pnpm validate:site"
---

<objective>
Add a Phase 2 release gate that validates the built domain pages locally and in GitHub Pages CI.

Purpose: Make the new domain architecture trustworthy by testing the emitted artifacts that GitHub Pages will actually publish, not just the source templates.
Output: A `validate:phase2` dist-audit script, an aggregate site validation command, and CI wiring that blocks deploys on Phase 1 or Phase 2 regressions.
</objective>

<execution_context>
@/Users/jstepek/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jstepek/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-domain-hubs-supporting-work/02-RESEARCH.md
@.planning/phases/02-domain-hubs-supporting-work/02-VALIDATION.md
@package.json
@.github/workflows/deploy.yml
@scripts/validate-phase1.mjs
@src/pages/domains/[slug].astro
@src/components/domains/DomainPage.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement a fast dist-first validator for the five domain pages</name>
  <files>scripts/validate-phase2.mjs</files>
  <action>Create a Node-based validation script that runs after `astro build` and inspects the emitted domain HTML in `dist/`. Assert that all five route artifacts exist, each page has a non-empty `<title>` and canonical URL, the shared template markers for page, thesis, supporting work, rendered supporting entries, proof link, and back-home link are present, the explicit back-home link resolves to the base-aware home path, and each page exposes at least one outward proof link with an absolute `http(s)` URL. Keep the validator fast, fail clearly, and do not make network requests or require every supporting item to have public proof.</action>
  <verify>`pnpm astro check && pnpm astro build && node ./scripts/validate-phase2.mjs` passes locally.</verify>
  <done>The repo has a Phase 2 artifact audit that proves the built domain hubs exist and carry the required structure.</done>
</task>

<task type="auto">
  <name>Task 2: Add local Phase 2 and aggregate site validation scripts</name>
  <files>package.json</files>
  <action>Add `validate:phase2` for the new dist audit and add an aggregate `validate:site` command that runs both Phase 1 and Phase 2 validators after build. Keep the scripts simple and future-friendly so later phases can extend the same aggregate gate instead of rewriting workflow steps again.</action>
  <verify>`pnpm astro check && pnpm astro build && pnpm validate:site` passes locally.</verify>
  <done>Local validation now exposes one repeatable command that covers the full site state through the end of Phase 2.</done>
</task>

<task type="auto">
  <name>Task 3: Update the GitHub Pages workflow to enforce the combined validation gate</name>
  <files>.github/workflows/deploy.yml</files>
  <action>Update the existing GitHub Pages workflow so it continues to install dependencies, run `pnpm astro check`, run the production build, and then run the aggregate site validation before uploading `dist` to Pages. Keep the single deploy flow, preserve the official Pages actions, and do not add remote proof-link checks that could make deploys flaky.</action>
  <verify>The workflow YAML references `pnpm astro check`, `pnpm astro build`, `pnpm validate:site`, `upload-pages-artifact`, and `deploy-pages` in the correct order.</verify>
  <done>CI now blocks deployment on missing domain artifacts, missing structural markers, or broken proof-link wiring in the built site.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm astro check && pnpm astro build && pnpm validate:site` succeeds
- [ ] `pnpm validate:phase2` inspects built `dist` HTML for all five domains and fails on missing artifacts, title/canonical wiring, wrong back-home `href`, missing thesis marker, missing supporting-work marker, missing rendered supporting entries, or missing proof-link presence
- [ ] Proof-link validation is structural only: require absolute `http(s)` URLs, but do not depend on remote availability
- [ ] `.github/workflows/deploy.yml` runs the same check, build, and aggregate validation sequence before `upload-pages-artifact` and `deploy-pages`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 exits with a repeatable local and CI artifact gate that proves the new domain hubs as built output, not just as source templates
</success_criteria>

<output>
After completion, create `.planning/phases/02-domain-hubs-supporting-work/02-03-SUMMARY.md`
</output>
