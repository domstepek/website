---
phase: 02-domain-hubs-supporting-work
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/data/domains/analytics.ts
  - src/data/domains/infrastructure.ts
  - src/data/domains/ai-ml.ts
  - src/data/domains/product.ts
  - src/data/domains/developer-experience.ts
  - src/components/domains/DomainPage.astro
  - src/styles/global.css
autonomous: true
requirements:
  - DOMN-02
  - DOMN-03
  - DOMN-04
user_setup: []
must_haves:
  truths:
    - Each domain page opens with a distinct first-person thesis, short bullets, and an explicit scope line that makes the boundary obvious without reading the whole page.
    - Supporting work reads as curated stacked evidence with one-line context and inline proof links, not as a generic repo dump, dense prose block, or Phase 4-style flagship section.
    - Presentation changes stay text-first and incremental: longer headings, supporting lists, related-domain cross-links, and focus states remain readable on mobile and desktop without rewriting the homepage or adding card-heavy UI.
  artifacts:
    - path: src/data/domains/analytics.ts
      provides: Finalized analytics thesis, scope, and supporting-work curation
      contains: "supportingWork"
    - path: src/data/domains/infrastructure.ts
      provides: Finalized infrastructure thesis, scope, and supporting-work curation
      contains: "supportingWork"
    - path: src/data/domains/ai-ml.ts
      provides: Finalized ai / ml thesis, scope, and supporting-work curation
      contains: "supportingWork"
    - path: src/data/domains/product.ts
      provides: Finalized product thesis, scope, and supporting-work curation
      contains: "supportingWork"
    - path: src/data/domains/developer-experience.ts
      provides: Longest-title domain entry and final supporting-work curation
      contains: "supportingWork"
    - path: src/components/domains/DomainPage.astro
      provides: Shared markup for supporting-work items, inline proof links, and optional overlap notes
      contains: "supporting work"
    - path: src/styles/global.css
      provides: Domain-page spacing and readability polish for real Phase 2 copy
      min_lines: 80
    - path: src/styles/global.css
      provides: Domain-page specific layout or spacing hooks
      contains: ".domain-page"
    - path: src/styles/global.css
      provides: Supporting-work grouping and link rhythm
      contains: ".supporting-work"
  key_links:
    - from: src/components/domains/DomainPage.astro
      to: src/styles/global.css
      via: Uses shared class hooks for domain-page spacing, headings, and supporting-entry rhythm
      pattern: "domain-page|supporting-work"
    - from: src/components/domains/DomainPage.astro
      to: src/lib/paths.ts
      via: Renders explicit home navigation and any related-domain cross-links with base-aware helpers
      pattern: "homePath|domainPath"
    - from: src/pages/domains/[slug].astro
      to: src/components/domains/DomainPage.astro
      via: Continues to render all five routes through the same polished template
      pattern: "DomainPage"
---

<objective>
Turn the baseline domain routes into convincing thesis pages with curated supporting work and light presentation polish.

Purpose: Strengthen the visitor-facing clarity of the five domain hubs so each page feels distinct, scannable, and evidence-backed without widening scope into Phase 3 or Phase 4.
Output: Polished thesis, scope, and supporting-work copy across all five domain modules plus small template and CSS refinements tuned to the real content.
</objective>

<execution_context>
@/Users/jstepek/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jstepek/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-domain-hubs-supporting-work/02-CONTEXT.md
@.planning/phases/02-domain-hubs-supporting-work/02-RESEARCH.md
@.planning/phases/02-domain-hubs-supporting-work/02-VALIDATION.md
@src/data/domains/types.ts
@src/data/domains/index.ts
@src/components/domains/DomainPage.astro
@src/pages/domains/[slug].astro
@src/styles/global.css
@src/pages/index.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sharpen thesis openings and boundary lines so the domains feel distinct</name>
  <files>src/data/domains/analytics.ts, src/data/domains/infrastructure.ts, src/data/domains/ai-ml.ts, src/data/domains/product.ts, src/data/domains/developer-experience.ts</files>
  <action>Revisit the five domain modules and tighten the `thesis`, `belongsHere`, `scope`, `summary`, and `seoDescription` fields until the difference between `analytics`, `infrastructure`, `ai / ml`, `product`, and `developer experience` is obvious from the top of each page. Keep the copy in the site's direct lowercase voice, use concrete problem language instead of broad portfolio adjectives, and do not pull homepage framing or flagship-story detail into this plan.</action>
  <verify>`pnpm astro check` passes and each domain module still satisfies the shared TypeScript contract.</verify>
  <done>The top of each domain page clearly explains what belongs there and what does not.</done>
</task>

<task type="auto">
  <name>Task 2: Curate supporting work into scan-friendly evidence with inline proof links</name>
  <files>src/data/domains/analytics.ts, src/data/domains/infrastructure.ts, src/data/domains/ai-ml.ts, src/data/domains/product.ts, src/data/domains/developer-experience.ts, src/components/domains/DomainPage.astro</files>
  <action>Edit the domain modules and shared template so supporting work reads as moderately curated stacked entries rather than a repo dump. Each entry should answer "what was this?" in one compact line before the visitor clicks, keep one primary proof link by default with an optional second link only when it materially helps, and use brief related-domain mentions instead of duplicating full entries across pages. Ensure every page still has at least one outward proof link somewhere, even though some individual supporting items may remain proofless.</action>
  <verify>`pnpm astro build` succeeds and each built domain page includes a visible `supporting work` section, at least one supporting item, and at least one outward proof link somewhere on the page.</verify>
  <done>The domain hubs read like curated evidence pages instead of placeholders or future case-study shells.</done>
</task>

<task type="auto">
  <name>Task 3: Add a minimal Phase 2 style pass for longer headings and denser lists</name>
  <files>src/components/domains/DomainPage.astro, src/styles/global.css</files>
  <action>Add only the CSS and small markup hooks needed for real domain copy: better wrapping for long titles like `developer experience`, section spacing between the thesis opening and supporting work, supporting-entry grouping, and comfortable link and focus rhythm on mobile and desktop. Keep the presentation text-forward, avoid cards or grids, and do not modify `src/pages/index.astro` or invent a second visual system for domain pages.</action>
  <verify>`pnpm astro build` succeeds, and a browser or built-page QA pass confirms the longest title, the longest supporting-work list, and keyboard focus states remain comfortable at mobile and desktop widths.</verify>
  <done>The shared domain pattern can hold real content without feeling cramped or visually noisy.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm astro check && pnpm astro build` succeeds
- [ ] Each domain page opens with a distinct thesis, short bullets, and an explicit scope line
- [ ] Each domain page has a curated supporting-work section with at least one public proof link somewhere on the page
- [ ] Long headings, stacked supporting entries, and focusable links remain readable on mobile and desktop widths
- [ ] CSS and template polish stay limited to domain routes and do not rewrite the homepage or add flagship sections
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The five domain pages now read like clear thesis pages with curated evidence, and the implementation still respects the Phase 2 boundary
</success_criteria>

<output>
After completion, create `.planning/phases/02-domain-hubs-supporting-work/02-02-SUMMARY.md`
</output>
