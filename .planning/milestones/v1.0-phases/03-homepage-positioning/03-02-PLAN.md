---
phase: 03-homepage-positioning
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/data/home.ts
  - src/data/domains/analytics.ts
  - src/data/domains/infrastructure.ts
  - src/data/domains/ai-ml.ts
  - src/data/domains/product.ts
  - src/data/domains/developer-experience.ts
  - src/components/home/HomePage.astro
  - src/styles/global.css
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
user_setup: []
must_haves:
  truths:
    - From the first screen, a visitor can tell that Dom works across analytics, infrastructure, ai / ml, product, and developer experience without having to open the domain pages first.
    - The homepage domain links read as the primary next step using short, distinct one-line previews from the shared domain data rather than a generic project grid, repo dump, or flagship section.
    - The homepage stays minimal and readable on mobile and desktop while still giving the hero, domain links, contact cluster, and freshness note enough visual hierarchy.
  artifacts:
    - path: src/data/home.ts
      provides: Tightened homepage framing copy and freshness wording
      exports:
        - homePage
    - path: src/data/domains/analytics.ts
      provides: Homepage-friendly analytics summary line
      contains: "summary"
    - path: src/data/domains/infrastructure.ts
      provides: Homepage-friendly infrastructure summary line
      contains: "summary"
    - path: src/data/domains/ai-ml.ts
      provides: Homepage-friendly ai / ml summary line
      contains: "summary"
    - path: src/data/domains/product.ts
      provides: Homepage-friendly product summary line
      contains: "summary"
    - path: src/data/domains/developer-experience.ts
      provides: Homepage-friendly developer experience summary line
      contains: "summary"
    - path: src/components/home/HomePage.astro
      provides: First-screen homepage section order and rendered domain-link list
      contains: "data-home-domain-link"
    - path: src/styles/global.css
      provides: Home-page spacing, hierarchy, and responsive layout polish
      contains: ".home-page"
    - path: src/styles/global.css
      provides: Dedicated homepage domain-navigation styling
      contains: ".home-page__domains"
  key_links:
    - from: src/components/home/HomePage.astro
      to: src/data/home.ts
      via: Uses the shared homepage framing and freshness copy in the visible page flow
      pattern: "homePage"
    - from: src/components/home/HomePage.astro
      to: src/data/domains/index.ts
      via: Renders the homepage domain previews from the ordered shared registry
      pattern: "domains"
    - from: src/components/home/HomePage.astro
      to: src/styles/global.css
      via: Uses home-page specific class hooks for hero rhythm, domain stack, and link-group spacing
      pattern: "home-page"
---

<objective>
Sharpen the homepage copy, domain previews, and text-first layout so the first screen carries the right positioning burden.

Purpose: Make the new homepage structure convincing to visitors without widening the scope into flagship proof, visuals, or Phase 5 personal-context material.
Output: Refined homepage framing, clearer one-line domain previews, and minimal CSS or markup polish tuned to the real homepage content.
</objective>

<execution_context>
@/Users/jstepek/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jstepek/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-homepage-positioning/03-RESEARCH.md
@.planning/phases/03-homepage-positioning/03-VALIDATION.md
@src/data/home.ts
@src/data/domains/index.ts
@src/data/domains/analytics.ts
@src/data/domains/infrastructure.ts
@src/data/domains/ai-ml.ts
@src/data/domains/product.ts
@src/data/domains/developer-experience.ts
@src/components/home/HomePage.astro
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten the homepage framing so the first screen explains Dom's scope quickly</name>
  <files>src/data/home.ts, src/components/home/HomePage.astro</files>
  <action>Refine the homepage eyebrow, heading, lead paragraph, contact labels, and freshness wording until the first screen clearly frames the site as a domain-first portfolio for systems, products, and tooling. Keep the tone direct and lowercase, keep the copy short enough to scan quickly, and do not let the hero turn into a broad manifesto or resume summary.</action>
  <verify>`pnpm astro check` passes and the homepage copy still compiles cleanly through the shared data and component layer.</verify>
  <done>The first screen communicates the scope of the site without requiring a visitor to read deeply or click away first.</done>
</task>

<task type="auto">
  <name>Task 2: Refine the five domain preview lines for homepage scanning</name>
  <files>src/data/domains/analytics.ts, src/data/domains/infrastructure.ts, src/data/domains/ai-ml.ts, src/data/domains/product.ts, src/data/domains/developer-experience.ts</files>
  <action>Review all five domain modules and tighten each `summary` field until the homepage has five intentional one-line previews that sound distinct from one another. Keep those summary lines consistent with the Phase 2 thesis boundaries, short enough to work as homepage navigation copy, and free of project lists or flagship detail. Some modules may only need light edits, but all five summaries should be deliberately checked and left homepage-ready.</action>
  <verify>`pnpm astro check && pnpm astro build` succeed and the built homepage still renders five domain previews from the shared registry without type or route regressions.</verify>
  <done>The homepage can preview all five domains with short, distinct routing lines drawn from the shared domain data.</done>
</task>

<task type="auto">
  <name>Task 3: Add a minimal layout pass for homepage hierarchy and scanability</name>
  <files>src/components/home/HomePage.astro, src/styles/global.css</files>
  <action>Add only the markup hooks and CSS needed for the real homepage content: hero spacing, domain-link stacking, contact-cluster rhythm, freshness-note placement, and comfortable mobile and desktop wraps. Keep the presentation text-forward, avoid cards or heavy grids, and preserve the existing site shell and typography baseline instead of inventing a second visual system.</action>
  <verify>`pnpm astro check && pnpm astro build` succeed and the built homepage remains readable on mobile and desktop widths without layout regressions.</verify>
  <done>The homepage feels intentional and easy to scan without losing the minimal style established earlier in the project.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm astro check && pnpm astro build` succeeds
- [ ] The first screen clearly signals analytics, infrastructure, ai / ml, product, and developer experience work
- [ ] The five rendered domain preview lines stay short, distinct, and aligned with the Phase 2 domain boundaries
- [ ] The five domain links feel like the primary internal next step rather than a generic gallery or repo list
- [ ] Contact and freshness content remain visible in the main homepage flow
- [ ] Homepage styling stays text-first and does not introduce flagship sections, screenshots, or heavy interaction
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The homepage now reads like a strong front door for the domain-first site instead of a temporary shell with generic copy
</success_criteria>

<output>
After completion, create `.planning/phases/03-homepage-positioning/03-02-SUMMARY.md`
</output>
