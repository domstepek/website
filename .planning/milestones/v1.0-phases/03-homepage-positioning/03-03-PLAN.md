---
phase: 03-homepage-positioning
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - scripts/validate-phase3.mjs
  - package.json
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
user_setup: []
must_haves:
  truths:
    - `pnpm validate:phase3` fails if the built homepage artifact is missing or if the page lacks the homepage hero, five domain links, contact links, or freshness marker.
    - The homepage validator inspects emitted `dist/index.html` and checks structural link shape only: base-aware internal domain hrefs, absolute `https` URLs for GitHub and LinkedIn, and a `mailto:` URL for email.
    - The aggregate site validation command covers Phases 1 through 3 through `pnpm validate:site`, so the existing Pages workflow automatically blocks deployment on homepage regressions.
  artifacts:
    - path: scripts/validate-phase3.mjs
      provides: Dist-first homepage artifact assertions
    - path: scripts/validate-phase3.mjs
      provides: Homepage artifact existence checks
      contains: "dist/index.html"
    - path: scripts/validate-phase3.mjs
      provides: Homepage domain-link marker assertions
      contains: "data-home-domain-link"
    - path: scripts/validate-phase3.mjs
      provides: Homepage contact-link marker assertions
      contains: "data-home-contact-link"
    - path: scripts/validate-phase3.mjs
      provides: Homepage freshness-marker assertions
      contains: "data-home-freshness"
    - path: package.json
      provides: Phase 3 validation script
      contains: "\"validate:phase3\""
    - path: package.json
      provides: Aggregate site validation script widened through Phase 3
      contains: "\"validate:site\""
  key_links:
    - from: scripts/validate-phase3.mjs
      to: dist/index.html
      via: Parses the built homepage artifact instead of source files
      pattern: "index\\.html"
    - from: scripts/validate-phase3.mjs
      to: src/components/home/HomePage.astro
      via: Reads the stable homepage `data-*` markers emitted into built HTML
      pattern: "data-home-hero|data-home-domain-link|data-home-contact-link|data-home-freshness"
    - from: package.json
      to: scripts/validate-phase1.mjs
      via: Keeps the aggregate site gate running the existing Phase 1 validator
      pattern: "validate:phase1"
    - from: package.json
      to: scripts/validate-phase2.mjs
      via: Keeps the aggregate site gate running the existing Phase 2 validator
      pattern: "validate:phase2"
    - from: package.json
      to: scripts/validate-phase3.mjs
      via: Adds homepage validation to the shared site-level release gate
      pattern: "validate:phase3"
---

<objective>
Add a dist-first homepage validator and extend the aggregate site release gate through Phase 3.

Purpose: Make the new homepage trustworthy by validating the emitted artifact that GitHub Pages will publish, not just the source files that generated it.
Output: A `validate:phase3` script plus an updated `validate:site` command that catches homepage regressions before deployment.
</objective>

<execution_context>
@/Users/jstepek/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jstepek/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-homepage-positioning/03-RESEARCH.md
@.planning/phases/03-homepage-positioning/03-VALIDATION.md
@package.json
@scripts/validate-phase1.mjs
@scripts/validate-phase2.mjs
@src/components/home/HomePage.astro
@src/data/home.ts
@src/data/domains/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement a fast dist-first validator for the built homepage</name>
  <files>scripts/validate-phase3.mjs</files>
  <action>Create a Node-based validation script that runs after `astro build` and inspects `dist/index.html`. Assert that the homepage artifact exists, has a non-empty `<title>` and canonical URL, renders the homepage hero marker, exposes exactly five domain links through the stable homepage markers, includes visible contact-link markers for GitHub, LinkedIn, and email, and includes a non-empty freshness marker. Validate internal domain hrefs against the base-aware expected paths and validate external link shape structurally only: `https` for GitHub and LinkedIn, `mailto:` for email. Do not make network requests.</action>
  <verify>`pnpm astro check && pnpm astro build && node ./scripts/validate-phase3.mjs` passes locally.</verify>
  <done>The repo has a homepage artifact audit that proves the built homepage structure and link wiring are present in emitted HTML.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 3 validation to the aggregate site gate</name>
  <files>package.json</files>
  <action>Add `validate:phase3` and extend `validate:site` so the existing Phase 1 and Phase 2 validators continue to run and the new homepage validator is included in the same aggregate command. Keep the script chain simple so the current GitHub Pages workflow automatically picks up the new gate without branching into a second CI path.</action>
  <verify>`pnpm astro check && pnpm astro build && pnpm validate:site` passes locally.</verify>
  <done>Local and CI release validation now covers homepage regressions through the end of Phase 3.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm astro check && pnpm astro build && pnpm validate:site` succeeds
- [ ] `pnpm validate:phase3` inspects built `dist/index.html` and fails on missing hero, missing or wrong domain links, missing contact links, or missing freshness marker
- [ ] External-link validation is structural only: require `https` for GitHub and LinkedIn and `mailto:` for email, but do not depend on remote availability
- [ ] The aggregate site validator continues to cover Phases 1 and 2 while adding Phase 3, so the current Pages workflow automatically inherits the homepage gate
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 3 exits with a repeatable local and CI artifact gate that proves the homepage as built output, not just as source templates
</success_criteria>

<output>
After completion, create `.planning/phases/03-homepage-positioning/03-03-SUMMARY.md`
</output>
