---
phase: 05-personal-context-notes
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/paths.ts
  - src/data/home.ts
  - src/data/personal.ts
  - src/components/home/HomePage.astro
  - src/components/personal/PersonalPage.astro
  - src/pages/about.astro
  - src/styles/global.css
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
user_setup: []
must_haves:
  truths:
    - The site gets one dedicated personal surface at `/about/` with a light local path into `/notes/`, while the homepage stays domain-first and only adds a short secondary teaser that routes into the fuller profile.
    - `how i work`, `open to`, and `resume` remain three distinct, scannable sections on the same personal page, with explicit systems, product, and collaboration coverage plus selectively-open opportunity framing.
    - A visitor can reach the resume in one click from the site and land on a stable on-page resume section without leaving the repo-owned site surface.
  artifacts:
    - path: src/lib/paths.ts
      provides: Base-aware helpers for the about page, notes routes, and the resume-section anchor path
      exports:
        - aboutPath
        - notesPath
        - notePath
        - resumePath
    - path: src/data/home.ts
      provides: Compact homepage teaser copy that keeps personal framing secondary to the domain map
      contains: "personalTeaser"
    - path: src/data/personal.ts
      provides: Structured personal-page copy, resume-section content, and SEO fields for the dedicated about surface
      contains: "resume"
    - path: src/components/personal/PersonalPage.astro
      provides: Shared personal-page rendering with stable profile markers plus an anchorable resume section for later validation
      contains: "data-resume-section"
    - path: src/components/personal/PersonalPage.astro
      provides: A lightweight on-page notes entry point that keeps the notes area easy to find without changing the global shell
      contains: "data-personal-notes-link"
    - path: src/components/home/HomePage.astro
      provides: Secondary homepage teaser that links into the about page and direct resume anchor without replacing the domain map
      contains: "data-home-personal-teaser"
  key_links:
    - from: src/pages/about.astro
      to: src/components/personal/PersonalPage.astro
      via: Keeps the new personal route thin and consistent with the shared route-component pattern
      pattern: "PersonalPage"
    - from: src/components/personal/PersonalPage.astro
      to: src/data/personal.ts
      via: Renders the profile sections from one typed content module instead of hardcoding long copy in the route
      pattern: "howIWork|openTo|resume"
    - from: src/components/home/HomePage.astro
      to: src/lib/paths.ts
      via: Uses shared base-aware helpers for the about and resume-anchor links from the homepage teaser
      pattern: "aboutPath|resumePath"
    - from: src/components/home/HomePage.astro
      to: src/data/home.ts
      via: Keeps teaser copy in the existing homepage data module instead of scattering homepage-only strings into templates
      pattern: "personalTeaser"
    - from: src/components/personal/PersonalPage.astro
      to: src/lib/paths.ts
      via: Uses shared base-aware helpers for the notes index link and the resume anchor from the personal page
      pattern: "notesPath|resumePath"
---

<objective>
Create the dedicated personal surface, wire in resume access, and add a light homepage teaser without diluting the domain-first architecture.

Purpose: Satisfy the Phase 5 profile requirements through one shared about-page pattern and base-aware links, not a homepage rewrite or shell-nav expansion.
Output: A new `about` route, structured personal data, a secondary homepage teaser, and GitHub Pages-safe resume access ready for Phase 5 validation.
</objective>

<execution_context>
@/Users/jstepek/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jstepek/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-personal-context-notes/05-CONTEXT.md
@.planning/phases/05-personal-context-notes/05-RESEARCH.md
@.planning/phases/05-personal-context-notes/05-VALIDATION.md
@src/lib/paths.ts
@src/data/home.ts
@src/components/home/HomePage.astro
@src/components/layout/BaseLayout.astro
@src/pages/index.astro
@src/pages/domains/[slug].astro
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend shared path helpers and model the personal-content contract</name>
  <files>src/lib/paths.ts, src/data/home.ts, src/data/personal.ts</files>
  <action>Add `aboutPath`, `notesPath`, `notePath()`, and `resumePath` to `src/lib/paths.ts`, with `resumePath` resolving to the dedicated resume section anchor on `/about/` so the requirement stays one-click accessible without a separate asset dependency. Extend `src/data/home.ts` with a compact `personalTeaser` object and small direct-resume CTA copy, then create `src/data/personal.ts` with typed lowercase copy for `lead`, `howIWork`, `openTo`, `resume`, and `seo`. Keep `howIWork` explicitly split into systems, product, and collaboration, keep `openTo` concrete about full-time plus contract or fractional work, and shape `resume` as structured on-page content instead of assuming a user-supplied PDF already exists.</action>
  <verify>
    <automated>`pnpm check`</automated>
  </verify>
  <done>The shared Phase 5 data and path contracts exist before any new route markup is introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Build the dedicated about route through a shared component and the existing layout</name>
  <files>src/components/personal/PersonalPage.astro, src/pages/about.astro, src/styles/global.css</files>
  <action>Create `PersonalPage.astro` so it renders a short lead plus three distinct sections: `how i work`, `open to`, and `resume`, plus a small local link into `/notes/` that keeps the writing surface easy to find without expanding the homepage. Emit stable `data-personal-page`, `data-how-i-work`, `data-how-i-work-systems`, `data-how-i-work-product`, `data-how-i-work-collaboration`, `data-open-to`, `data-resume-section`, and `data-personal-notes-link` markers, give the resume section a stable `id` that `resumePath` can target directly, and keep the page segmented, plain-language, and text-forward rather than biographical or card-heavy. Create `src/pages/about.astro` as a thin route under `BaseLayout`, add only the supporting global CSS needed for readable section rhythm, and ensure the about page itself acts as the canonical v1 personal surface.</action>
  <verify>
    <automated>`pnpm check && pnpm build`</automated>
  </verify>
  <done>`/about/` exists as the main personal surface and satisfies the profile structure in one place.</done>
</task>

<task type="auto">
  <name>Task 3: Add a secondary homepage teaser that points visitors into the personal layer</name>
  <files>src/data/home.ts, src/components/home/HomePage.astro, src/styles/global.css</files>
  <action>Insert one short teaser section below the domain navigation and above the contact or freshness cluster, using 1 to 2 sentences, a clear link to `/about/`, and a small direct resume link to `resumePath`. Keep the teaser visually and structurally secondary to the five domain links so the homepage remains the domain map first. Emit `data-home-personal-teaser`, `data-home-personal-link`, and `data-home-resume-link` markers.</action>
  <verify>
    <automated>`pnpm check && pnpm build`</automated>
  </verify>
  <done>The homepage improves personal discoverability without turning Phase 5 into a homepage rewrite.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm check && pnpm build` succeeds
- [ ] `src/pages/about.astro` stays a thin route that renders `PersonalPage` through `BaseLayout`
- [ ] The homepage still renders the five domain links before exactly one secondary personal teaser block
- [ ] The built about page exposes non-empty markers for `how i work`, `systems`, `product`, `collaboration`, `open to`, and `resume`
- [ ] The about page exposes a base-aware notes entry point so `/notes/` is easy to find without adding shell navigation
- [ ] The resume is reachable through shared anchor path helpers and the homepage teaser without depending on a separate user-supplied asset or off-site document host
- [ ] Manual browser or tone checks for teaser hierarchy and about-page readability remain tracked in `05-VALIDATION.md`, not inside this autonomous plan
- [ ] No shell-nav rewrite, homepage hero rewrite, or blog-style personal expansion was introduced
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 5 exits this plan with one dedicated personal surface and a light homepage entry point that satisfy the profile requirements without weakening the domain-first information architecture
</success_criteria>

<output>
After completion, create `.planning/phases/05-personal-context-notes/05-01-SUMMARY.md`
</output>
