---
phase: 05-personal-context-notes
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/content.config.ts
  - src/content/notes/systems-over-abstractions.md
  - src/content/notes/keep-the-path-explicit.md
  - src/components/notes/NotesIndexPage.astro
  - src/components/notes/NotePage.astro
  - src/pages/notes/index.astro
  - src/pages/notes/[slug].astro
  - src/styles/global.css
autonomous: true
requirements:
  - NOTE-01
  - NOTE-02
user_setup: []
must_haves:
  truths:
    - The notes area behaves like lightweight field notes rather than a full blog or publishing platform.
    - Phase 5 ships with at least 2 starter field notes so the notes area reads like a real index instead of a single orphaned post.
    - The notes index is a simple reverse-chronological list where each entry shows title, summary, and date before sending the visitor into the detail page.
    - Each note opens to a plain, readable page with explicit title, date, and body markers, local back links, and metadata derived from the note itself instead of a second handwritten registry.
  artifacts:
    - path: src/content.config.ts
      provides: Typed notes collection schema with explicit summary and published-date fields
      contains: "published"
    - path: src/content/notes/systems-over-abstractions.md
      provides: A starter field note with frontmatter summary and plain Markdown body
      contains: "summary:"
    - path: src/content/notes/keep-the-path-explicit.md
      provides: A second starter field note with frontmatter summary and plain Markdown body
      contains: "summary:"
    - path: src/components/notes/NotesIndexPage.astro
      provides: Shared notes-index rendering with stable list markers
      contains: "data-notes-index"
    - path: src/components/notes/NotePage.astro
      provides: Shared note-detail rendering with stable title, date, and body markers
      contains: "data-note-body"
    - path: src/pages/notes/index.astro
      provides: Thin notes-index route under the shared layout contract
      contains: "NotesIndexPage"
    - path: src/pages/notes/[slug].astro
      provides: Static note-detail route generation from the collection
      contains: "getStaticPaths"
    - path: src/styles/global.css
      provides: Minimal note-index and note-prose treatments that preserve the text-forward visual system
      contains: ".notes-index"
  key_links:
    - from: src/pages/notes/index.astro
      to: src/components/notes/NotesIndexPage.astro
      via: Keeps the notes index route thin and consistent with the existing shared-component pattern
      pattern: "NotesIndexPage"
    - from: src/pages/notes/[slug].astro
      to: src/components/notes/NotePage.astro
      via: Keeps individual note routes thin while rendering collection entries through one shared page component
      pattern: "NotePage|render"
    - from: src/components/notes/NotesIndexPage.astro
      to: src/content.config.ts
      via: Queries the typed notes collection and sorts it explicitly by published date instead of relying on file order
      pattern: "getCollection(\"notes\")|published"
    - from: src/components/notes/NotePage.astro
      to: src/lib/paths.ts
      via: Uses base-aware local links back to the notes index and surrounding site routes
      pattern: "notesPath|homePath"
---

<objective>
Add the lightweight notes system with Markdown-backed content, thin routes, and just enough styling to keep the list and note bodies readable.

Purpose: Satisfy the Phase 5 notes requirements through a minimal static content pipeline that can grow later without becoming a blog platform in v1.
Output: A notes collection config, starter note content, index and detail routes, and text-forward styling ready for Phase 5 validation.
</objective>

<execution_context>
@/Users/jstepek/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jstepek/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-personal-context-notes/05-CONTEXT.md
@.planning/phases/05-personal-context-notes/05-RESEARCH.md
@.planning/phases/05-personal-context-notes/05-VALIDATION.md
@src/lib/paths.ts
@src/components/layout/BaseLayout.astro
@src/pages/index.astro
@src/pages/domains/[slug].astro
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the typed notes collection and seed it with real starter field notes</name>
  <files>src/content.config.ts, src/content/notes/systems-over-abstractions.md, src/content/notes/keep-the-path-explicit.md</files>
  <action>Add `src/content.config.ts` with a `notes` collection that validates `title`, `summary`, `published`, and optional `updated`, then create the two starter notes `systems-over-abstractions.md` and `keep-the-path-explicit.md` under `src/content/notes/` using flat slugs and plain Markdown bodies. Keep the frontmatter summaries intentional instead of auto-generating excerpts, write the entries as short field notes rather than essay-length posts, and do not introduce tags, categories, MDX, or a second manual registry file.</action>
  <verify>
    <automated>`pnpm check`</automated>
  </verify>
  <done>The repo has a typed multi-entry notes source that can drive both the index and the note detail routes.</done>
</task>

<task type="auto">
  <name>Task 2: Build shared notes index and detail rendering through thin routes</name>
  <files>src/components/notes/NotesIndexPage.astro, src/components/notes/NotePage.astro, src/pages/notes/index.astro, src/pages/notes/[slug].astro</files>
  <action>Create `NotesIndexPage.astro` and `NotePage.astro`, then wire thin routes for `/notes/` and `/notes/[slug]/` through `BaseLayout`. Query the notes via `getCollection("notes")`, sort them explicitly by `published` descending before rendering, and emit stable `data-notes-index`, `data-note-item`, `data-note-title`, `data-note-summary`, `data-note-date`, and `data-note-link` markers on the index plus `data-note-page`, `data-note-title`, `data-note-date`, and `data-note-body` markers on the detail page. Render machine-readable `datetime` attributes for note dates, derive page metadata from note frontmatter, and keep local back links simple and base-aware.</action>
  <verify>
    <automated>`pnpm check && pnpm build`</automated>
  </verify>
  <done>The notes area exists as a static index plus individual note pages without inventing a blog engine.</done>
</task>

<task type="auto">
  <name>Task 3: Add only the minimal note styling needed for readable index and detail pages</name>
  <files>src/styles/global.css</files>
  <action>Extend `src/styles/global.css` with the smallest set of note-specific rules needed for the notes index list, metadata line, note body wrapper, and any Markdown elements used by the starter notes. Preserve the site's existing text-forward rhythm, keep the notes pages plain and scan-friendly, and avoid blog chrome such as tags, cards, archive widgets, pagination, featured-image slots, or heavy article templates. Leave subjective readability and mobile-browser judgment to `05-VALIDATION.md`.</action>
  <verify>
    <automated>`pnpm check && pnpm build`</automated>
  </verify>
  <done>The notes UI feels like lightweight field notes instead of a mini publishing platform.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm check && pnpm build` succeeds
- [ ] `getCollection("notes")` results are explicitly sorted by `published` descending before rendering
- [ ] `dist/notes/index.html` exists, renders at least 2 note items, and shows title, summary, and date for each one
- [ ] Every rendered note link resolves to a static built note artifact under `dist/notes/<slug>/index.html`
- [ ] Every built note page exposes non-empty `data-note-title`, `data-note-date`, and `data-note-body` markers
- [ ] Manual browser or readability checks for note scanning and prose comfort remain tracked in `05-VALIDATION.md`, not inside this autonomous plan
- [ ] No tags, search, pagination, MDX-only features, or blog-platform chrome were introduced
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 5 exits this plan with a lightweight notes index, at least 2 starter field notes, and individual note pages that satisfy the note requirements without expanding into a larger publishing system
</success_criteria>

<output>
After completion, create `.planning/phases/05-personal-context-notes/05-02-SUMMARY.md`
</output>
