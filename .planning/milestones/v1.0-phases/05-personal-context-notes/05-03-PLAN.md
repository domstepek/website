---
phase: 05-personal-context-notes
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - scripts/validate-phase5.mjs
  - package.json
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - NOTE-01
  - NOTE-02
user_setup: []
must_haves:
  truths:
    - The Phase 5 release gate fails if the built site is missing the homepage teaser, about-page profile sections, notes entry point, one-click resume access, notes index, or note detail pages.
    - The Phase 5 release gate catches broken note ordering or broken note-detail routes by following rendered note links in built output.
    - One shared site-validation command proves the published site through the end of Phase 5 without requiring a separate CI path.
  artifacts:
    - path: scripts/validate-phase5.mjs
      provides: Dist-first validator for the personal and notes surfaces introduced in Phase 5
    - path: scripts/validate-phase5.mjs
      provides: About-page notes-entry checks, note-link traversal, resume-anchor checks, and rendered-date ordering assertions
      contains: "data-note-link"
    - path: package.json
      provides: Dedicated Phase 5 validation script
      contains: "\"validate:phase5\""
    - path: package.json
      provides: Aggregate site validation widened through the end of Phase 5
      contains: "\"validate:site\""
  key_links:
    - from: scripts/validate-phase5.mjs
      to: dist/index.html
      via: Parses the built homepage artifact for the secondary personal teaser and base-aware about or resume links
      pattern: "data-home-personal-teaser|data-home-personal-link|data-home-resume-link"
    - from: scripts/validate-phase5.mjs
      to: dist/about/index.html
      via: Parses the built about artifact for profile markers, the notes entry path, and the anchorable resume section
      pattern: "data-personal-page|data-how-i-work|data-open-to|data-resume-section|data-personal-notes-link"
    - from: scripts/validate-phase5.mjs
      to: dist/notes/index.html
      via: Follows rendered note links and validates ordering plus note-page artifacts from built HTML
      pattern: "data-note-item|data-note-date|data-note-link"
    - from: package.json
      to: scripts/validate-phase1.mjs
      via: Keeps the earlier phase validators in the shared site gate while adding the new Phase 5 audit
      pattern: "validate:phase1|validate:phase2|validate:phase3|validate:phase4|validate:phase5"
---

<objective>
Add a dist-first Phase 5 validator and widen the shared site release gate through the new profile and notes surfaces.

Purpose: Prove that the emitted GitHub Pages artifacts actually contain the personal teaser, about page, resume path, notes index, and note detail routes that Phase 5 introduces.
Output: A `validate:phase5` script plus an updated `validate:site` command that catches broken Phase 5 structure before deploy.
</objective>

<execution_context>
@/Users/jstepek/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jstepek/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-personal-context-notes/05-CONTEXT.md
@.planning/phases/05-personal-context-notes/05-RESEARCH.md
@.planning/phases/05-personal-context-notes/05-VALIDATION.md
@package.json
@scripts/validate-phase3.mjs
@scripts/validate-phase4.mjs
@src/components/home/HomePage.astro
@src/components/personal/PersonalPage.astro
@src/components/notes/NotesIndexPage.astro
@src/components/notes/NotePage.astro
@src/lib/paths.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement a dist-first validator for the homepage teaser, about page, and notes artifacts</name>
  <files>scripts/validate-phase5.mjs</files>
  <action>Create a Node-based validator that runs after `astro build` and inspects `dist/index.html`, `dist/about/index.html`, and `dist/notes/index.html`. Assert that the homepage exposes exactly one `data-home-personal-teaser` block plus base-aware about and resume-anchor links, that the about page exposes non-empty `data-personal-page`, `data-how-i-work`, `data-how-i-work-systems`, `data-how-i-work-product`, `data-how-i-work-collaboration`, `data-open-to`, `data-resume-section`, and `data-personal-notes-link` markers, and that the about-page notes link resolves to the canonical `/notes/` path while the homepage resume link targets the canonical about-page resume anchor. Parse the rendered note items from the built notes index, require at least 2 starter notes with non-empty title, summary, and date text, validate reverse-chronological ordering from rendered `datetime` attributes, follow each built note link into its `dist` artifact, and assert each note page exposes non-empty `data-note-title`, `data-note-date`, and `data-note-body` markers plus page metadata derived from note content.</action>
  <verify>
    <automated>`pnpm check && pnpm build && node ./scripts/validate-phase5.mjs`</automated>
  </verify>
  <done>The repo has a Phase 5 artifact audit that proves the personal and notes surfaces exist in emitted output and keep the expected structure.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 5 validation to the aggregate site release gate</name>
  <files>package.json</files>
  <action>Add `validate:phase5` and extend `validate:site` so the existing Phase 1 through Phase 4 validators still run before the new Phase 5 validator. Keep the command chain simple and compatible with the existing CI flow so no GitHub Actions rewrite is needed to pick up the stronger gate.</action>
  <verify>
    <automated>`pnpm check && pnpm build && pnpm validate:site`</automated>
  </verify>
  <done>The shared local and CI release gate now validates the site through the end of Phase 5.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm check && pnpm build && pnpm validate:site` succeeds
- [ ] `pnpm validate:phase5` inspects built HTML and built assets rather than raw source templates or Markdown files
- [ ] The validator fails on missing personal teaser markers, missing about-page profile or notes-entry markers, a broken resume anchor path, missing note links, or broken note detail artifacts
- [ ] Reverse-chronological note ordering is validated from rendered `datetime` values, not collection or file order
- [ ] The aggregate site gate now runs Phase 1 through Phase 5 in one command without a separate CI workflow path
- [ ] Manual browser smoke checks and subjective UX judgments remain tracked in `05-VALIDATION.md`, not as blockers for autonomous plan execution
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 5 exits with a repeatable dist-first release gate that proves the personal and notes surfaces as published artifacts instead of relying on source assumptions
</success_criteria>

<output>
After completion, create `.planning/phases/05-personal-context-notes/05-03-SUMMARY.md`
</output>
